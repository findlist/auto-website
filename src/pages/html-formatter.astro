---
import BaseLayout, { type SiteMeta } from '../layouts/BaseLayout.astro';
import HtmlFormatterTool from '../components/HtmlFormatterTool.tsx';

const meta: SiteMeta = {
  title: 'HTML 格式化与压缩工具 - 美化 / 压缩 / 校验三合一',
  description:
    '在线 HTML 格式化与压缩工具：美化、压缩、校验三合一，基于原生 DOMParser 容错解析，可选缩进风格与保留注释。全本地处理，零广告零追踪。',
  jsonLd: {
    '@context': 'https://schema.org',
    '@type': 'WebApplication',
    name: 'HTML 格式化与压缩工具',
    description: '在线 HTML 格式化、压缩与校验工具，支持缩进、保留注释、小写化标签、移除可选闭合标签等选项，全本地处理。',
    applicationCategory: 'DeveloperApplication',
    operatingSystem: 'Any (Web Browser)',
    inLanguage: 'zh-CN',
    offers: {
      '@type': 'Offer',
      price: '0',
      priceCurrency: 'CNY',
    },
  },
};
---
<BaseLayout meta={meta}>
  <section class="container">
    <div class="page-hero page-hero--tool">
      <h1>HTML 格式化与压缩工具</h1>
      <p>
        HTML 代码的美化、压缩与校验三合一。美化模式按层级缩进对齐，压缩模式移除注释与空白减小体积，校验模式输出解析统计与可疑问题清单。基于浏览器原生 DOMParser，符合 HTML5 容错规则，自动修复未闭合标签。数据仅在浏览器本地处理。
      </p>
    </div>

    <!-- client:load 让 React 岛屿在页面加载时立即水合，保证交互即时可用 -->
    <HtmlFormatterTool client:load />

    <!-- 常见问题：补充 SEO 内容 -->
    <section class="json-faq" aria-labelledby="faq-title">
      <h2 id="faq-title">常见问题</h2>
      <details>
        <summary>什么是 HTML 格式化？为什么需要它？</summary>
        <p>HTML 格式化（也称美化或 Pretty Print）是将结构混乱、缩进缺失的 HTML 代码重新按层级缩进对齐的过程。它不改变代码语义，只调整可读性。常见场景：①从压缩工具还原源码；②阅读服务器模板渲染的 HTML；③整理 IDE 自动生成或复制粘贴的代码；④代码审查时统一缩进风格。本工具支持 2 空格、4 空格、Tab 三种缩进风格，可自定义保留注释、保留空白、小写化标签等选项。</p>
      </details>
      <details>
        <summary>HTML 压缩能减小多少体积？</summary>
        <p>压缩效果取决于原代码的空白与注释量。典型场景：含大量缩进与注释的源码压缩后可减小 30-50% 体积；已经是单行的代码压缩收益有限（约 5-10%）。本工具压缩模式提供「移除注释」「合并空白」「省略可选闭合标签」三个选项。注意：压缩后的代码可读性差，仅用于生产部署，开发环境应保留美化版本便于调试。</p>
      </details>
      <details>
        <summary>什么是 void elements？为什么它们不需要闭合标签？</summary>
        <p>HTML5 规范定义了 14 个 <strong>void elements</strong>（空元素）：area、base、br、col、embed、hr、img、input、link、meta、param、source、track、wbr。这些元素不能有子节点，因此不需要闭合标签（即不需要 <code>&lt;br&gt;&lt;/br&gt;</code> 这样的写法）。XHTML 时代流行 <code>&lt;br /&gt;</code> 自闭合写法，HTML5 中两种写法都合法，但 <code>&lt;br&gt;</code> 更简洁。本工具统一输出 <code>&lt;br&gt;</code> 形式。</p>
      </details>
      <details>
        <summary>哪些闭合标签可以省略？安全吗？</summary>
        <p>HTML5 规范允许省略多个元素的闭合标签，包括 <code>&lt;/html&gt;</code>、<code>&lt;/head&gt;</code>、<code>&lt;/body&gt;</code>、<code>&lt;/li&gt;</code>、<code>&lt;/p&gt;</code>、<code>&lt;/td&gt;</code>、<code>&lt;/tr&gt;</code>、<code>&lt;/option&gt;</code> 等。浏览器会自动补全闭合。省略后体积可减小 5-10%，但可读性变差且容易引发嵌套错误（如 <code>&lt;p&gt;</code> 内嵌 <code>&lt;div&gt;</code> 会自动断开 <code>&lt;p&gt;</code>）。本工具压缩模式提供「省略可选闭合标签」选项，默认关闭，仅在追求极致体积时开启。</p>
      </details>
      <details>
        <summary>pre、textarea、script、style 内容会被缩进吗？</summary>
        <p>不会。这四个元素属于 <strong>rawtext elements</strong>，其内容应原样保留：①<code>&lt;pre&gt;</code> 与 <code>&lt;textarea&gt;</code> 中的空白与换行有显示意义（用户看到的就是源码原样）；②<code>&lt;script&gt;</code> 与 <code>&lt;style&gt;</code> 内容是代码，缩进会破坏语法（如模板字符串中的换行）。本工具遇到这些元素时，会原样输出内容，不递归缩进、不归一化空白。这也是为什么格式化 JavaScript 应使用专门的 JS 格式化工具。</p>
      </details>
      <details>
        <summary>为什么标签名要小写化？</summary>
        <p>HTML5 规范虽然大小写不敏感（<code>&lt;DIV&gt;</code>、<code>&lt;Div&gt;</code>、<code>&lt;div&gt;</code> 等价），但推荐使用小写形式。原因：①XHTML 与 XML 严格区分大小写，小写形式兼容性更好；②代码风格统一更易读；③工具链（如 lint、IDE 提示）默认小写；④搜索引擎爬虫更友好。本工具默认开启小写化，可在选项中关闭以保留原始大小写。</p>
      </details>
      <details>
        <summary>校验模式会执行哪些检查？</summary>
        <p>校验模式基于浏览器原生 DOMParser 解析后输出统计与问题清单：①统计元素数、文本节点数、注释数、属性数、最大嵌套深度；②检测未编码的 <code>&lt;</code> 与 <code>&amp;</code> 字符（应写为 <code>&amp;lt;</code> 与 <code>&amp;amp;</code>）；③检测重复属性（同元素同名属性，浏览器只保留第一个）；④报告解析错误。注意 HTML5 是容错规范，浏览器会自动修复未闭合标签、错误嵌套等问题，校验模式不会阻止解析，只会提示潜在问题。</p>
      </details>
      <details>
        <summary>与 HTML 实体编解码工具有何区别？</summary>
        <p>两者互补但不同：<strong>HTML 实体工具</strong>（<a href="/html-entities">/html-entities</a>）处理「文本 ↔ HTML 实体」的字符级转换，如 <code>&amp;</code> ↔ <code>&amp;amp;</code>，用于在 HTML 文档中安全显示特殊字符与防御 XSS；<strong>HTML 格式化工具</strong>（本工具）处理「HTML 代码结构」的层级缩进与压缩，不改变字符内容。典型流程：先用 HTML 实体工具编码用户输入，再将整个 HTML 文档用本工具美化或压缩。两者形成「HTML 处理」工具矩阵。</p>
      </details>
      <details>
        <summary>与 Markdown 工具有何联动？</summary>
        <p>Markdown 渲染产物是 HTML 字符串。<a href="/markdown">Markdown 预览工具</a>渲染后的 HTML 通常缩进混乱、缺少换行，可以复制到本工具美化模式，得到层级清晰的可读 HTML 源码用于检查或调试。反向场景：本工具压缩后的 HTML 可粘贴到 Markdown 的 HTML 块中（Markdown 支持嵌入原生 HTML），减小嵌入体积。</p>
      </details>
      <details>
        <summary>数据会上传到服务器吗？</summary>
        <p>不会。本工具完全在浏览器内运行，所有解析、美化、压缩、校验逻辑由 JavaScript 在你本地执行，输入内容不会发送到任何服务器。</p>
      </details>
    </section>

    <!-- 相关工具：同矩阵互链 + 跨矩阵精选互补，提升站内导航密度 -->
    <section class="related-tools" aria-labelledby="related-title">
      <h2 id="related-title">相关工具</h2>
      <ul class="related-tools__list">
        <li><a href="/css-formatter">CSS 格式化与压缩工具</a> —— 同系列 CSS 代码美化与压缩</li>
        <li><a href="/js-formatter">JavaScript 格式化与压缩工具</a> —— 同系列 JS 代码美化与压缩</li>
        <li><a href="/html-entities">HTML 实体编解码</a> —— HTML 特殊字符转义与反转义</li>
        <li><a href="/html-to-markdown">HTML 转 Markdown 转换器</a> —— 网页内容提取与博客迁移</li>
        <li><a href="/markdown">Markdown 预览器</a> —— Markdown 渲染与预览</li>
        <li><a href="/base64">Base64 编解码</a> —— HTML 内容编码传输</li>
        <li><a href="/diff">文本对比工具</a> —— HTML 代码版本差异对比</li>
      </ul>
    </section>

    <!-- 相关博客：工具页反向内链，引导用户深度阅读（脚本批量生成，请勿手动修改） -->
    <section class="related-blogs" aria-labelledby="related-blogs-title">
      <h2 id="related-blogs-title">相关博客</h2>
      <ul class="related-blogs__list">
        <li>
          <a href="/blog/html-formatting-minify-guide">HTML 格式化与压缩原理：从解析模型到 minify 算法</a><span class="related-blogs__desc">深入解析 HTML 格式化与压缩原理：HTML5 解析模型与容错规则、空白语义、void elements 与 rawtext 元素、递归缩进美化与 minify 算法，附原…</span>
        </li>
      </ul>
    </section>
  </section>
</BaseLayout>

<style is:global>
  /* HTML 格式化工具专属样式：复用 .jsontool 基础样式，仅补充差异部分 */
  .htmlfmt__seg {
    display: inline-flex;
    gap: 4px;
  }

  .htmlfmt__options {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    padding: 8px 12px;
    margin-bottom: 12px;
    background: var(--color-bg-soft, #f6f8fa);
    border-radius: 6px;
  }

  .htmlfmt__toggle {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 13px;
    color: var(--color-text-soft);
    cursor: pointer;
    user-select: none;
  }

  .htmlfmt__toggle input {
    margin: 0;
    cursor: pointer;
  }

  .htmlfmt__toggle select {
    padding: 2px 6px;
    font-size: 13px;
    border: 1px solid var(--color-border, #d0d7de);
    border-radius: 4px;
    background: var(--color-bg, #fff);
    cursor: pointer;
  }

  .htmlfmt__field {
    margin-bottom: 12px;
  }

  .htmlfmt__textarea {
    width: 100%;
    min-height: 200px;
    padding: 10px 12px;
    font-family: 'Cascadia Code', 'Fira Code', 'JetBrains Mono', 'Consolas', 'Menlo', monospace;
    font-size: 13px;
    line-height: 1.5;
    border: 1px solid var(--color-border, #d0d7de);
    border-radius: 6px;
    background: var(--color-bg, #fff);
    color: var(--color-text, #1f2328);
    resize: vertical;
    box-sizing: border-box;
  }

  .htmlfmt__textarea:focus {
    outline: 2px solid var(--color-accent, #0969da);
    outline-offset: -1px;
  }

  .htmlfmt__output {
    min-height: 200px;
    max-height: 500px;
    overflow: auto;
    padding: 10px 12px;
    margin: 0;
    font-family: 'Cascadia Code', 'Fira Code', 'JetBrains Mono', 'Consolas', 'Menlo', monospace;
    font-size: 13px;
    line-height: 1.5;
    background: var(--color-bg-soft, #f6f8fa);
    border: 1px solid var(--color-border, #d0d7de);
    border-radius: 6px;
    white-space: pre-wrap;
    word-break: break-all;
  }

  /* 校验报告模式：等宽字体，按行显示 */
  .htmlfmt__output--report {
    white-space: pre-wrap;
    word-break: normal;
  }

  .htmlfmt__ratio {
    margin-left: 8px;
    padding: 2px 6px;
    font-size: 12px;
    color: #fff;
    background: #1f883d;
    border-radius: 4px;
  }

  /* 移动端响应式 */
  @media (max-width: 640px) {
    .htmlfmt__options {
      gap: 8px;
      padding: 6px 8px;
    }

    .htmlfmt__toggle {
      font-size: 12px;
    }

    .htmlfmt__textarea,
    .htmlfmt__output {
      font-size: 12px;
      min-height: 150px;
    }
  }
</style>
