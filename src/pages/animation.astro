---
import BaseLayout, { type SiteMeta } from '../layouts/BaseLayout.astro';
import AnimationTool from '../components/AnimationTool.tsx';

const meta: SiteMeta = {
  title: 'CSS animation 动画生成器 - 在线 @keyframes 关键帧可视化工具',
  description:
    '在线 CSS animation 动画生成器：可视化编辑 @keyframes 关键帧与八大属性，8 组预设动画一键复制。全本地处理，零广告零追踪。',
  jsonLd: {
    '@context': 'https://schema.org',
    '@type': 'WebApplication',
    name: 'CSS animation 动画生成器',
    description: '在线 CSS animation 动画生成器，可视化编辑 @keyframes 关键帧，实时预览，一键复制 CSS 代码。',
    applicationCategory: 'DeveloperApplication',
    operatingSystem: 'Any (Web Browser)',
    inLanguage: 'zh-CN',
    offers: {
      '@type': 'Offer',
      price: '0',
      priceCurrency: 'CNY',
    },
  },
};
---
<BaseLayout meta={meta}>
  <section class="container">
    <div class="page-hero page-hero--tool">
      <h1>CSS animation 动画生成器</h1>
      <p>
        可视化生成 CSS <code>animation</code> 动画：编辑 <code>@keyframes</code> 关键帧，每帧支持
        translate、rotate、scale、opacity 属性；完整配置 duration、timing-function、delay、
        iteration-count、direction、fill-mode、play-state 八大属性。实时预览动画效果，一键复制
        <code>@keyframes</code> 与 <code>animation</code> CSS 代码。内置弹跳、旋转、脉冲、淡入、
        淡出、右滑入、抖动、摇摆 8 组预设。所有处理在浏览器本地完成。
      </p>
    </div>

    <AnimationTool client:load />

    <section class="json-faq" aria-labelledby="faq-title">
      <h2 id="faq-title">常见问题</h2>
      <details>
        <summary>CSS animation 属性包含哪些子属性？</summary>
        <p>
          <code>animation</code> 是 8 个子属性简写，顺序为：
          <br />① <code>animation-name</code>：引用的 @keyframes 名称
          <br />② <code>animation-duration</code>：一次动画周期时长，如 2s
          <br />③ <code>animation-timing-function</code>：缓动函数，如 ease、linear、cubic-bezier()
          <br />④ <code>animation-delay</code>：动画开始前的延迟，如 0.5s
          <br />⑤ <code>animation-iteration-count</code>：播放次数，如 3 或 infinite
          <br />⑥ <code>animation-direction</code>：方向，normal / reverse / alternate / alternate-reverse
          <br />⑦ <code>animation-fill-mode</code>：填充模式，none / forwards / backwards / both
          <br />⑧ <code>animation-play-state</code>：播放状态，running / paused
          <br />简写示例：<code>animation: anim 2s ease 0s infinite alternate both running;</code>
        </p>
      </details>
      <details>
        <summary>@keyframes 关键帧的百分比是什么含义？</summary>
        <p>
          <code>@keyframes</code> 中的百分比表示动画周期的时间节点：0% 是周期起点（等价于 from），
          100% 是周期终点（等价于 to），中间可插入任意百分比关键帧（如 25%、50%、75%）。
          浏览器会在相邻关键帧之间自动插值，产生平滑过渡。例如弹跳动画在 0% 与 100% 处
          translateY=0（落地），50% 处 translateY=-40px（最高点），浏览器自动计算中间的位移。
          关键帧的百分比值不要求连续或有序，浏览器按数值升序应用。
        </p>
      </details>
      <details>
        <summary>animation-timing-function 的缓动函数有哪些？</summary>
        <p>
          缓动函数控制动画在每个关键帧之间的速度曲线：
          <br />① <code>linear</code>：匀速，适合旋转、进度条
          <br />② <code>ease</code>：默认值，慢入快出慢收
          <br />③ <code>ease-in</code>：慢入快出，适合淡出
          <br />④ <code>ease-out</code>：快入慢出，适合淡入
          <br />⑤ <code>ease-in-out</code>：慢入慢出，适合弹跳、脉冲
          <br />⑥ <code>cubic-bezier(n, n, n, n)</code>：自定义贝塞尔曲线，如
          <code>cubic-bezier(0.68, -0.55, 0.27, 1.55)</code> 产生回弹 overshoot 效果
          <br />注意：timing-function 作用于"每个关键帧之间"，而非整个动画周期。可在关键帧内
          单独声明 <code>animation-timing-function</code> 控制单段曲线。
        </p>
      </details>
      <details>
        <summary>animation-fill-mode 的 forwards 和 backwards 有什么区别？</summary>
        <p>
          <code>fill-mode</code> 控制动画在执行前后的样式应用：
          <br /><strong>none（默认）</strong>：动画执行前用元素原始样式，执行后回到原始样式。
          淡入动画结束后元素会"消失"（回到 opacity:0 的原始状态）。
          <br /><strong>forwards</strong>：动画结束后保持最后一帧的样式。淡入动画结束后元素保持
          opacity:1 的可见状态。
          <br /><strong>backwards</strong>：动画开始前（delay 期间）就应用第一帧的样式。淡入动画
          在延迟期间元素就已是 opacity:0 的不可见状态，避免闪烁。
          <br /><strong>both</strong>：同时应用 forwards 与 backwards 行为，是最安全的入场动画选择。
          <br />本工具的"淡入"预设使用 forwards，"右滑入"预设使用 both。
        </p>
      </details>
      <details>
        <summary>animation-direction 的 alternate 是什么效果？</summary>
        <p>
          <code>direction</code> 控制动画播放方向：
          <br />① <code>normal</code>：每次循环都从 0% 播放到 100%（正向）
          <br />② <code>reverse</code>：每次循环都从 100% 播放到 0%（反向）
          <br />③ <code>alternate</code>：奇数次正向、偶数次反向，形成往返效果。如左右摇摆动画
          正向滑出后反向滑回，无需定义反向关键帧。
          <br />④ <code>alternate-reverse</code>：奇数次反向、偶数次正向，与 alternate 起始方向相反。
          <br />alternate 系列要求 <code>iteration-count</code> 大于 1 或为 infinite 才能体现效果，
          单次播放时与 normal/reverse 无差异。
        </p>
      </details>
      <details>
        <summary>transform 与 animation 如何配合？</summary>
        <p>
          <code>transform</code> 是 CSS 属性，<code>animation</code> 是驱动属性随时间变化的机制。
          在 <code>@keyframes</code> 中声明 <code>transform: translate() rotate() scale()</code>，
          浏览器会在关键帧之间自动插值，产生平滑动画。transform 是 GPU 加速的合成层属性，
          不触发重排，是动画的首选属性（优于 top/left/width/height）。本工具每个关键帧支持
          translateX/Y、rotate、scale 四个 transform 子属性 + opacity，覆盖 90% 的常见动画需求。
          配合站点现有的 <a href="/transform">CSS transform 可视化工具</a>可先调好静态变换值，
          再到本工具组合成动画。
        </p>
      </details>
      <details>
        <summary>animation 与 transition 有什么区别？</summary>
        <p>
          两者都产生过渡效果，但触发机制与控制粒度不同：
          <br /><strong>transition</strong>：被动触发，仅在属性值变化时（如 :hover、class 切换）
          播放一次过渡，无法定义中间关键帧，只能从起值到终值线性插值。适合简单的悬停反馈、
          状态切换。
          <br /><strong>animation</strong>：主动驱动，通过 @keyframes 定义多个关键帧与缓动曲线，
          可循环播放（infinite）、控制方向（alternate）、暂停（paused）。适合加载动画、
          入场效果、持续循环动效。
          <br />简单悬停用 transition，复杂动效用 animation，两者经常配合使用。
        </p>
      </details>
      <details>
        <summary>数据会上传到服务器吗？</summary>
        <p>
          不会。本工具完全在浏览器内运行，所有关键帧编辑、属性配置、动画预览与 CSS 生成由
          JavaScript 在你本地执行，不会发送到任何服务器。@keyframes 规则通过动态注入
          <code>&lt;style&gt;</code> 标签生效，全程零网络请求，可离线使用。
        </p>
      </details>
    </section>

    <!-- 相关工具：动效组同矩阵互链 + 现代CSS跨组互补 -->
    <section class="related-tools" aria-labelledby="related-title">
      <h2 id="related-title">相关工具</h2>
      <ul class="related-tools__list">
        <li><a href="/filter">CSS 滤镜生成器</a> — 滤镜关键帧动画</li>
        <li><a href="/transform">CSS 变换生成器</a> — 变换关键帧动画</li>
        <li><a href="/transition">CSS 过渡生成器</a> — 过渡与动画协同</li>
        <li><a href="/interpolate-size">CSS interpolate-size 尺寸插值生成器</a> — 尺寸动画</li>
        <li><a href="/starting-style">CSS @starting-style 入场动画生成器</a> — 入场动画</li>
        <li><a href="/view-transition">CSS view-transition 视图过渡生成器</a> — 视图过渡动画</li>
        <li><a href="/scroll-driven">CSS 滚动驱动动画生成器</a> — 滚动驱动关键帧</li>
      </ul>
    </section>

    <!-- 相关博客：工具页反向内链，引导用户深度阅读（脚本批量生成，请勿手动修改） -->
    <section class="related-blogs" aria-labelledby="related-blogs-title">
      <h2 id="related-blogs-title">相关博客</h2>
      <ul class="related-blogs__list">
        <li>
          <a href="/blog/animation-guide">CSS animation 动画完全指南：@keyframes 关键帧、八大子属性、缓动函数与性能优化</a><span class="related-blogs__desc">深入解析 CSS animation 动画系统：@keyframes 关键帧、八大子属性、cubic-bezier 缓动曲线、fill-mode 填充模式与 GPU 合成层优…</span>
        </li>
      </ul>
    </section>
  </section>
</BaseLayout>

<style is:global>
  /* CSS animation 动画生成器专属样式 */
  .amt {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  /* 预览区：舞台 + 重启按钮 */
  .amt__preview-wrap {
    padding: 16px;
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.03));
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
  }

  /* 预览舞台：网格背景，可视化位移轨迹 */
  .amt__preview-stage {
    position: relative;
    width: 100%;
    height: 220px;
    border-radius: var(--radius-md);
    background-color: var(--color-bg);
    background-image:
      linear-gradient(var(--color-border) 1px, transparent 1px),
      linear-gradient(90deg, var(--color-border) 1px, transparent 1px);
    background-size: 20px 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  /* 动画方块：主色背景，圆角，实时应用 animation */
  .amt__box {
    width: 64px;
    height: 64px;
    border-radius: 12px;
    background: linear-gradient(135deg, #2b6cff 0%, #7a4cff 100%);
    box-shadow: 0 4px 16px rgba(43, 108, 255, 0.3);
  }

  .amt__btn {
    padding: 6px 14px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    background: var(--color-bg);
    color: var(--color-text);
    font-size: 13px;
    cursor: pointer;
    transition: background var(--transition-fast, 0.15s), border-color var(--transition-fast, 0.15s);
  }

  .amt__btn:hover:not(:disabled) {
    border-color: var(--color-primary, #2b6cff);
  }

  .amt__btn:disabled {
    opacity: 0.4;
    cursor: not-allowed;
  }

  .amt__btn--restart {
    margin-top: 12px;
    width: 100%;
    padding: 8px;
    border-color: var(--color-primary, #2b6cff);
    color: var(--color-primary, #2b6cff);
    font-weight: 500;
  }

  .amt__btn--restart:hover {
    background: var(--color-primary, #2b6cff);
    color: #fff;
  }

  .amt__btn--preset {
    font-size: 13px;
  }

  .amt__btn--reset {
    color: var(--color-danger, #e53e3e);
    border-color: var(--color-danger, #e53e3e);
  }

  .amt__btn--reset:hover {
    background: var(--color-danger, #e53e3e);
    color: #fff;
  }

  .amt__btn--copy {
    font-size: 13px;
  }

  .amt__btn--add {
    font-size: 12px;
    padding: 4px 10px;
    border-color: var(--color-primary, #2b6cff);
    color: var(--color-primary, #2b6cff);
  }

  .amt__btn--del {
    padding: 3px 8px;
    font-size: 12px;
    color: var(--color-text-soft);
  }

  /* 预设按钮组 */
  .amt__presets {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 8px;
    padding: 14px 16px;
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.03));
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
  }

  .amt__presets-label {
    font-size: 14px;
    font-weight: 600;
    color: var(--color-text);
  }

  /* 通用面板容器 */
  .amt__panel {
    padding: 16px 18px;
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.03));
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
  }

  .amt__panel-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 14px;
  }

  .amt__panel-title {
    font-size: 14px;
    font-weight: 600;
    color: var(--color-text);
  }

  /* animation 属性网格：4 列布局 */
  .amt__config-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 14px;
  }

  .amt__field {
    display: flex;
    flex-direction: column;
    gap: 6px;
    font-size: 12px;
    color: var(--color-text-soft);
  }

  .amt__field span {
    font-weight: 500;
  }

  .amt__field input[type='range'] {
    width: 100%;
    accent-color: var(--color-primary, #2b6cff);
  }

  .amt__text-input,
  .amt__select {
    width: 100%;
    padding: 5px 8px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    background: var(--color-bg);
    color: var(--color-text);
    font-size: 13px;
    font-family: var(--font-mono);
  }

  .amt__select {
    font-family: var(--font-body, inherit);
  }

  /* @keyframes 关键帧列表 */
  .amt__keyframes {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .amt__kf {
    padding: 12px 14px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    background: var(--color-bg);
  }

  .amt__kf-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 10px;
  }

  .amt__kf-offset {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 13px;
    font-weight: 600;
    color: var(--color-primary, #2b6cff);
  }

  .amt__kf-offset input {
    width: 56px;
    padding: 3px 6px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    background: var(--color-bg);
    color: var(--color-text);
    font-family: var(--font-mono);
    font-size: 13px;
    text-align: center;
  }

  /* 单帧属性网格：5 列布局 */
  .amt__kf-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 12px;
  }

  .amt__kf-field {
    display: flex;
    flex-direction: column;
    gap: 4px;
    font-size: 11px;
    color: var(--color-text-soft);
  }

  .amt__kf-field input[type='range'] {
    width: 100%;
    accent-color: var(--color-primary, #2b6cff);
  }

  /* CSS 代码输出 */
  .amt__output {
    padding: 16px;
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.03));
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
  }

  .amt__output-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 10px;
  }

  .amt__output-label {
    font-size: 14px;
    font-weight: 600;
    color: var(--color-text);
  }

  .amt__code {
    margin: 0;
    padding: 12px 14px;
    background: var(--color-bg);
    border-radius: var(--radius-sm);
    font-family: var(--font-mono);
    font-size: 14px;
    color: var(--color-text);
    white-space: pre-wrap;
    word-break: break-all;
    line-height: 1.5;
  }

  /* 暗色模式适配 */
  @media (prefers-color-scheme: dark) {
    .amt__kf,
    .amt__code,
    .amt__text-input,
    .amt__select,
    .amt__kf-offset input {
      background: rgba(255, 255, 255, 0.04);
    }
    .amt__preview-stage {
      background-color: rgba(255, 255, 255, 0.02);
    }
  }

  /* 移动端响应式：4 列 → 2 列，5 列 → 2 列 */
  @media (max-width: 768px) {
    .amt__config-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .amt__kf-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 414px) {
    .amt__presets {
      flex-direction: column;
      align-items: stretch;
    }
    .amt__presets-label {
      text-align: center;
    }
    .amt__config-grid {
      grid-template-columns: 1fr;
    }
    .amt__kf-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
