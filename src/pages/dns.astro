---
import BaseLayout, { type SiteMeta } from '../layouts/BaseLayout.astro';
import DnsTool from '../components/DnsTool.tsx';

const meta: SiteMeta = {
  title: 'DNS 查询工具 - DoH 在线 A/AAAA/MX/TXT/CNAME 多记录类型查询',
  description:
    '在线 DNS 查询工具：基于 DNS over HTTPS（DoH）协议直连 Cloudflare / Google / DNS.SB 公共服务器，支持 A / AAAA / CNAME / MX / TXT / NS / SOA / PTR / CAA / SRV / DS / DNSKEY / TLSA / HTTPS / SVCB / NAPTR 共 16 种记录类型查询，含 DNSSEC 验证状态、TTL 解读、dig 风格文本导出。浏览器直连 DoH，查询不经本站服务器，零广告零追踪。',
  jsonLd: {
    '@context': 'https://schema.org',
    '@type': 'WebApplication',
    name: 'DNS 查询工具',
    description: '基于 DNS over HTTPS（DoH）协议的在线 DNS 查询工具，支持 16 种记录类型查询、DNSSEC 验证、TTL 解读与 dig 风格文本导出。',
    applicationCategory: 'DeveloperApplication',
    operatingSystem: 'Any (Web Browser)',
    inLanguage: 'zh-CN',
    offers: {
      '@type': 'Offer',
      price: '0',
      priceCurrency: 'CNY',
    },
  },
};
---
<BaseLayout meta={meta}>
  <section class="container">
    <div class="page-hero page-hero--tool">
      <h1>DNS 查询工具</h1>
      <p>
        基于 <strong>DNS over HTTPS（DoH）</strong>协议直连公共 DNS 服务器，
        支持 <strong>16 种记录类型</strong>查询：A / AAAA / CNAME / MX / TXT / NS / SOA /
        PTR / CAA / SRV / DS / DNSKEY / TLSA / HTTPS / SVCB / NAPTR。
        内置 <strong>3 个 DoH 服务商</strong>（Cloudflare / Google / DNS.SB）一键切换，
        实时显示<strong>DNSSEC 验证状态</strong>、TTL 与耗时，
        支持 <strong>dig 风格文本导出</strong>与原始 JSON 查看，方便复制分享。
        所有查询由浏览器直连 DoH 服务器，<strong>不经本站服务器</strong>，
        不记录任何查询内容。适用于域名解析排查、CDN 切换验证、邮件 MX/SPF/DKIM 调试、
        DNSSEC 部署验证、HTTP/3 升级提示分析、安全研究等场景。
      </p>
    </div>

    <!-- client:load 让 React 岛屿在页面加载时立即水合，保证交互即时可用 -->
    <DnsTool client:load />

    <!-- 常见问题：补充 SEO 内容 + 解决核心用户疑问 -->
    <section class="json-faq" aria-labelledby="faq-title">
      <h2 id="faq-title">常见问题</h2>
      <details>
        <summary>什么是 DNS over HTTPS（DoH）？和普通 DNS 查询有什么区别？</summary>
        <p>
          <strong>DNS over HTTPS（DoH，RFC 8484）</strong>是将传统 DNS 查询封装在
          HTTPS 请求中传输的协议，与传统 UDP/TCP DNS 相比有三个核心优势：<br />
          - <strong>加密传输</strong>：DNS 查询内容经过 TLS 加密，中间人无法窥探查询了什么域名<br />
          - <strong>防篡改</strong>：HTTPS 完整性保护防止 DNS 劫持与中间人攻击<br />
          - <strong>与正常 Web 流量混在一起</strong>：DoH 流量看起来就是普通 HTTPS 请求，
          难以单独识别与干扰<br />
          <strong>本工具的查询流程</strong>：浏览器 → HTTPS 加密 → DoH 服务器（如 1.1.1.1）
          → 递归 DNS 服务器 → 权威 DNS 服务器。返回结果同样经 HTTPS 加密回浏览器。
          传统 UDP DNS 查询走 53 端口明文，会被运营商、路由器、Wi-Fi 提供商看到与劫持。
        </p>
      </details>
      <details>
        <summary>查询真的不经本站服务器吗？数据安全吗？</summary>
        <p>
          <strong>是的，全部查询由浏览器直连 DoH 服务器，本站不参与查询链路</strong>。<br />
          - 浏览器调用原生 <code>fetch()</code> API 直接请求 Cloudflare / Google / DNS.SB 的 DoH 端点<br />
          - 本站仅托管前端页面与 JavaScript 代码，不部署任何后端转发服务<br />
          - 查询的域名、记录类型、DNSSEC 开关作为 URL 查询参数发给 DoH 服务器（HTTPS 加密）<br />
          - 历史查询仅保存在<strong>当前浏览器内存中</strong>（关页即清，不上传不持久化）<br />
          - 无 Cookie 追踪、无第三方统计、无广告<br />
          <strong>隐私建议</strong>：本工具内置的 3 个 DoH 服务商均明确承诺不记录查询者 IP，
          临时日志 24-48 小时内删除。如对隐私有更高要求，可使用 DNS.SB（亚太节点，
          无任何审查）或自建 DoH 服务。
        </p>
      </details>
      <details>
        <summary>各种记录类型分别是什么？什么时候用哪个？</summary>
        <p>
          常用记录类型按使用场景分组：<br />
          - <strong>网站访问</strong>：A（IPv4 地址）、AAAA（IPv6 地址）、CNAME（别名，常用于 CDN 接入）<br />
          - <strong>邮件服务</strong>：MX（邮件服务器优先级）、TXT（SPF/DKIM/DMARC 反垃圾配置）<br />
          - <strong>域名管理</strong>：NS（权威 DNS 服务器）、SOA（区域主服务器与全局参数）<br />
          - <strong>反向解析</strong>：PTR（IP 反查域名，输入 <code>1.0.0.127.in-addr.arpa</code> 格式）<br />
          - <strong>证书签发</strong>：CAA（限制 CA 证书签发权限，防止未授权 CA 签发证书）<br />
          - <strong>服务发现</strong>：SRV（服务端口与主机，SIP/XMPP/AD 常用）、HTTPS/SVCB
          （HTTP/3 升级提示、Aliasing）<br />
          - <strong>DNSSEC 链</strong>：DNSKEY（区域公钥）、DS（父区对子区 KSK 的签名）、TLSA
          （TLS 证书指纹绑定，DANE 增强）<br />
          <strong>选型提示</strong>：日常排查网站访问问题用 A/AAAA；邮件收发问题查 MX/TXT；
          CDN 切换验证查 CNAME；HTTP/3 是否启用查 HTTPS 记录；DNSSEC 部署状态查 DNSKEY/DS。
        </p>
      </details>
      <details>
        <summary>AD 和 CD 字段是什么？DNSSEC 验证状态怎么解读？</summary>
        <p>
          DoH 响应中的 AD / CD 字段反映 DNSSEC 验证状态：<br />
          - <strong>AD（Authenticated Data）= 1</strong>：DoH 服务器已对结果执行 DNSSEC 验证且数据可信。
          说明该域名已部署 DNSSEC，且签名链可信。<br />
          - <strong>AD = 0</strong>：结果未通过 DNSSEC 验证。可能原因：域名未部署 DNSSEC、
          DNSSEC 配置错误、签名链断裂。<br />
          - <strong>CD（Checking Disabled）= 1</strong>：本次查询主动关闭了 DNSSEC 校验。
          DoH 服务器不会验证 DNSSEC，直接返回原始数据。<br />
          <strong>本工具的「请求 DNSSEC 验证」开关控制 cd 参数</strong>：开启时 cd=0
          （DoH 服务器会验证 DNSSEC，可在 AD 位查看结果），关闭时 cd=1（不验证，
          适合调试 DNSSEC 链断裂问题，可看到原始未验证数据）。<br />
          <strong>调试建议</strong>：先开启 DNSSEC 验证查 DNSKEY/DS，确认链路完整后关闭验证对比
          原始数据，可定位签名失败的具体环节。
        </p>
      </details>
      <details>
        <summary>Status 返回 NXDOMAIN / SERVFAIL / REFUSED 分别是什么意思？</summary>
        <p>
          DoH 响应中的 Status 字段对应 DNS RCODE（RFC 1035）：<br />
          - <strong>NOERROR（0）</strong>：查询成功，至少有一条记录返回（Answer 非空）<br />
          - <strong>NXDOMAIN（3）</strong>：域名不存在。可能原因：域名未注册、已删除、拼写错误。<br />
          - <strong>SERVFAIL（2）</strong>：服务器失败。可能原因：域名 DNSSEC 配置错误、
          权威服务器宕机、网络故障。建议换 DoH 服务商重试确认。<br />
          - <strong>REFUSED（5）</strong>：查询被拒绝。可能原因：DoH 服务商策略限制（如内网专用记录）、
          频率限制触发。<br />
          - <strong>FORMERR（1）</strong>：请求格式错误。本工具不应出现，若出现请反馈。<br />
          - <strong>NOTIMP（4）</strong>：DoH 服务器不支持该记录类型。<br />
          <strong>排查思路</strong>：NXDOMAIN → 用 <code>whois</code> 查域名注册状态；
          SERVFAIL → 切换 DoH 服务商对比，若多服务商均 SERVFAIL 多为 DNSSEC 配置错误；
          REFUSED → 检查记录类型是否被服务商限制（如某些内网专用 TLD）。
        </p>
      </details>
      <details>
        <summary>TTL 是什么？为什么不同记录的 TTL 不一样？</summary>
        <p>
          <strong>TTL（Time To Live，生存时间）</strong>是 DNS 记录在递归解析器中的缓存秒数。
          TTL 越大缓存越久、查询越快但变更生效越慢；TTL 越小查询越慢但变更生效越快。<br />
          - <strong>A/AAAA 记录</strong>：通常 300-3600 秒（5-60 分钟），CDN 域名常设 60 秒以便快速切换<br />
          - <strong>MX/TXT 记录</strong>：通常 3600 秒（1 小时），邮件相关变更频率低<br />
          - <strong>NS 记录</strong>：通常 86400 秒（1 天），权威服务器变更极少<br />
          - <strong>SOA 记录</strong>：包含 7 个 TTL 字段（serial/refresh/retry/expire/minimum），
          控制从服务器同步策略<br />
          <strong>缓存层级</strong>：浏览器 DNS 缓存 → 操作系统 DNS 缓存 → 路由器缓存 →
          ISP 递归解析器缓存 → 权威服务器。每一层都会按 TTL 缓存，因此<strong>修改 DNS 后并非立即全球生效</strong>，
          需等所有层级 TTL 过期。降低 TTL 后再修改是切换前的标准做法。
        </p>
      </details>
      <details>
        <summary>为什么查询超时或失败？怎么排查？</summary>
        <p>
          浏览器直连 DoH 失败的常见原因与排查思路：<br />
          - <strong>网络不通</strong>：检查浏览器是否能正常访问其他网站；
          若国内网络下访问 Cloudflare / Google 偶发失败，可切换 DNS.SB 或多服务商重试<br />
          - <strong>DoH 服务商被屏蔽</strong>：部分网络环境会屏蔽特定 DoH 端点
          （如 1.1.1.1 在某些网络下不可达），切换其他服务商<br />
          - <strong>CORS 跨域拦截</strong>：本工具仅使用明确支持 CORS 的 DoH 服务商，
          不应出现此问题；若出现请反馈<br />
          - <strong>HTTPS 证书问题</strong>：系统时间不正确会导致 TLS 握手失败，
          检查系统时间是否准确<br />
          - <strong>域名格式错误</strong>：本工具会自动校验，但仍可粘贴 URL 自动截取域名部分<br />
          <strong>排查建议</strong>：先切换 3 个 DoH 服务商逐一尝试；
          若全部失败多为本地网络问题（防火墙、代理、DNS 污染），可尝试关闭代理重试；
          若仅个别服务商失败，多为该服务商节点不可达。
        </p>
      </details>
      <details>
        <summary>HTTPS / SVCB 记录是什么？和 HTTP/3 有什么关系？</summary>
        <p>
          <strong>HTTPS 记录（RFC 9460）</strong>是 SVCB 记录的特化版本，
          用于在 DNS 阶段告知客户端「该域名支持哪些 HTTPS 服务变体与参数」。<br />
          - <strong>AliasMode（priority=0）</strong>：类似 CNAME，将当前域名指向另一域名的 HTTPS 记录<br />
          - <strong>ServiceMode（priority&gt;0）</strong>：直接提供服务参数，如 HTTP/3 端口、
          ALPN 协议列表（h3/h2）、IPv4/IPv6 hints、ECH 公钥等<br />
          <strong>核心场景：HTTP/3 升级</strong>。传统 HTTP/3 升级需先 TCP 连接拿到
          <code>Alt-Svc</code> 头，存在一次额外往返；HTTPS 记录在 DNS 阶段就告知浏览器
          「该域名支持 h3 协议」，浏览器可直接发起 QUIC 连接，节省一次往返。<br />
          <strong>ECH（Encrypted Client Hello）</strong>：HTTPS 记录可携带 ECH 公钥，
          用于加密 TLS ClientHello 中的 SNI 字段，防止中间人识别访问的域名。<br />
          <strong>查询 Cloudflare / Google 的 HTTPS 记录</strong>可看到完整的 h3 / ECH 配置，
          这是现代 CDN 与浏览器协同优化的核心机制。
        </p>
      </details>
    </section>

    <section class="related-tools" aria-labelledby="related-title">
      <h2 id="related-title">相关工具</h2>
      <ul class="related-tools__list">
        <li><a href="/ip">IP 子网计算器</a> —— IPv4/IPv6 子网划分与 CIDR 计算</li>
        <li><a href="/tls">TLS 证书解析工具</a> —— 域名 HTTPS 证书与 DNS 协同调试</li>
        <li><a href="/http-status">HTTP 状态码</a> —— DNS 解析错误对应的 HTTP 错误</li>
        <li><a href="/http-headers">HTTP Header 解析</a> —— 响应头部中的 DNS 相关字段</li>
        <li><a href="/http-request">HTTP 请求代码生成器</a> —— 多语言 HTTP 客户端代码生成</li>
        <li><a href="/user-agent">User-Agent 解析器</a> —— 客户端 UA 字符串识别</li>
        <li><a href="/mime">MIME 类型查询</a> —— 文件扩展名与 MIME 类型映射</li>
        <li><a href="/punycode">Punycode 编解码</a> —— 国际化域名 IDN 与 ACE 转换</li>
      </ul>
    </section>

    <!-- 相关博客：工具页反向内链，引导用户深度阅读（脚本批量生成，请勿手动修改） -->
    <section class="related-blogs" aria-labelledby="related-blogs-title">
      <h2 id="related-blogs-title">相关博客</h2>
      <ul class="related-blogs__list">
        <li>
          <a href="/blog/dns-query-guide">DNS 查询实战：从 DoH 协议到 16 种记录类型与 DNSSEC 验证</a><span class="related-blogs__desc">系统讲解 DNS 基础架构与查询流程、16 种记录类型（A/AAAA/CNAME/MX/TXT/NS/SOA/PTR/CAA/SRV/DS/DNSKEY/TLSA/HTTPS…</span>
        </li>
      </ul>
    </section>
  </section>
</BaseLayout>

<style is:global>
  /* 工具页专属样式：dns 命名空间 */

  .dns__container {
    margin: 24px 0;
  }

  /* 预设场景按钮组 */
  .dns__presets {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 10px;
    margin-bottom: 16px;
    padding: 12px 14px;
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.03));
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md, 8px);
  }

  .dns__presets-label {
    font-size: 13px;
    font-weight: 600;
    color: var(--color-text-muted, #6b7280);
    white-space: nowrap;
  }

  .dns__presets-list {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .dns__preset-btn {
    padding: 5px 12px;
    border: 1px solid var(--color-border);
    border-radius: 16px;
    background: var(--color-bg);
    color: var(--color-text);
    font-size: 12px;
    cursor: pointer;
    transition: all 0.15s;
  }

  .dns__preset-btn:hover {
    border-color: var(--color-accent, #2563eb);
    color: var(--color-accent, #2563eb);
  }

  /* 主布局：左右两栏 */
  .dns__main {
    display: grid;
    grid-template-columns: 380px 1fr;
    gap: 16px;
    align-items: start;
  }

  .dns__panel,
  .dns__result {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md, 8px);
    background: var(--color-bg);
    padding: 16px;
  }

  .dns__panel-title {
    margin: 0 0 14px;
    font-size: 14px;
    font-weight: 700;
    color: var(--color-text);
    padding-bottom: 10px;
    border-bottom: 1px solid var(--color-border);
  }

  /* 表单字段 */
  .dns__field {
    margin-bottom: 14px;
  }

  .dns__field--inline {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
  }

  .dns__field-label {
    display: block;
    margin-bottom: 6px;
    font-size: 13px;
    font-weight: 600;
    color: var(--color-text);
  }

  .dns__field--inline .dns__field-label {
    margin-bottom: 0;
    flex: 0 0 auto;
  }

  .dns__input,
  .dns__select {
    width: 100%;
    box-sizing: border-box;
    padding: 8px 10px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm, 6px);
    background: var(--color-bg);
    color: var(--color-text);
    font-size: 13px;
    font-family: inherit;
  }

  .dns__input:focus,
  .dns__select:focus {
    outline: 2px solid var(--color-accent, #2563eb);
    outline-offset: -1px;
    border-color: var(--color-accent, #2563eb);
  }

  .dns__input[aria-invalid='true'] {
    border-color: #dc2626;
  }

  .dns__field-hint {
    margin: 6px 0 0;
    font-size: 12px;
    color: var(--color-text-muted, #6b7280);
    line-height: 1.5;
  }

  .dns__field-hint code {
    padding: 1px 4px;
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.05));
    border-radius: 3px;
    font-size: 11px;
  }

  .dns__field-error {
    margin: 6px 0 0;
    font-size: 12px;
    color: #dc2626;
  }

  /* 开关 */
  .dns__switch {
    position: relative;
    display: inline-block;
    width: 36px;
    height: 20px;
    flex-shrink: 0;
  }

  .dns__switch input {
    opacity: 0;
    width: 0;
    height: 0;
  }

  .dns__switch-slider {
    position: absolute;
    cursor: pointer;
    inset: 0;
    background: var(--color-border, #cbd5e1);
    border-radius: 20px;
    transition: 0.2s;
  }

  .dns__switch-slider::before {
    content: '';
    position: absolute;
    height: 14px;
    width: 14px;
    left: 3px;
    bottom: 3px;
    background: white;
    border-radius: 50%;
    transition: 0.2s;
  }

  .dns__switch input:checked + .dns__switch-slider {
    background: var(--color-accent, #2563eb);
  }

  .dns__switch input:checked + .dns__switch-slider::before {
    transform: translateX(16px);
  }

  .dns__switch input:focus-visible + .dns__switch-slider {
    outline: 2px solid var(--color-accent, #2563eb);
    outline-offset: 2px;
  }

  .dns__switch-label {
    font-size: 12px;
    color: var(--color-text-muted, #6b7280);
  }

  /* 查询按钮 */
  .dns__submit {
    width: 100%;
    padding: 10px 16px;
    border: none;
    border-radius: var(--radius-sm, 6px);
    background: var(--color-accent, #2563eb);
    color: white;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.15s;
  }

  .dns__submit:hover:not(:disabled) {
    background: var(--color-accent-hover, #1d4ed8);
  }

  .dns__submit:disabled {
    background: var(--color-text-muted, #9ca3af);
    cursor: not-allowed;
  }

  /* 历史记录 */
  .dns__history {
    margin-top: 16px;
    padding-top: 14px;
    border-top: 1px solid var(--color-border);
  }

  .dns__history-head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 8px;
  }

  .dns__history-head h3 {
    margin: 0;
    font-size: 13px;
    font-weight: 600;
    color: var(--color-text);
  }

  .dns__history-clear {
    border: none;
    background: none;
    color: var(--color-text-muted, #6b7280);
    font-size: 12px;
    cursor: pointer;
    padding: 2px 6px;
    border-radius: 4px;
  }

  .dns__history-clear:hover {
    color: #dc2626;
    background: rgba(220, 38, 38, 0.08);
  }

  .dns__history-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .dns__history-item {
    width: 100%;
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 6px 8px;
    border: 1px solid var(--color-border);
    border-radius: 4px;
    background: var(--color-bg);
    color: var(--color-text);
    font-size: 12px;
    cursor: pointer;
    transition: all 0.15s;
  }

  .dns__history-item:hover {
    border-color: var(--color-accent, #2563eb);
  }

  .dns__history-name {
    flex: 1;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    font-family: ui-monospace, 'SF Mono', Consolas, monospace;
  }

  .dns__history-type {
    font-size: 10px;
    padding: 1px 5px;
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.05));
    border-radius: 3px;
    color: var(--color-text-muted, #6b7280);
  }

  .dns__history-status {
    font-size: 10px;
    font-weight: 600;
  }

  .dns__history-status--ok {
    color: #16a34a;
  }

  .dns__history-status--fail {
    color: #dc2626;
  }

  /* 结果区状态：加载、空、错误 */
  .dns__loading,
  .dns__empty,
  .dns__error {
    padding: 32px 16px;
    text-align: center;
    color: var(--color-text-muted, #6b7280);
  }

  .dns__loading {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
  }

  .dns__spinner {
    width: 28px;
    height: 28px;
    border: 3px solid var(--color-border);
    border-top-color: var(--color-accent, #2563eb);
    border-radius: 50%;
    animation: dns-spin 0.8s linear infinite;
  }

  @keyframes dns-spin {
    to {
      transform: rotate(360deg);
    }
  }

  .dns__empty-hint {
    margin-top: 8px;
    font-size: 12px;
    opacity: 0.85;
  }

  .dns__error h3 {
    color: #dc2626;
    margin: 0 0 8px;
    font-size: 14px;
  }

  .dns__meta {
    margin: 12px 0 0;
    text-align: left;
    font-size: 12px;
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 4px 12px;
  }

  .dns__meta dt {
    color: var(--color-text-muted, #6b7280);
  }

  .dns__meta dd {
    margin: 0;
    word-break: break-all;
  }

  /* 查询成功结果 */
  .dns__success {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .dns__summary {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 8px;
    padding: 10px 12px;
    border-radius: var(--radius-sm, 6px);
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.03));
    border-left: 4px solid var(--color-accent, #2563eb);
  }

  .dns__summary--noerror {
    border-left-color: #16a34a;
  }

  .dns__summary--nxdomain {
    border-left-color: #dc2626;
  }

  .dns__summary--servfail,
  .dns__summary--refused {
    border-left-color: #d97706;
  }

  .dns__summary-item {
    display: flex;
    flex-direction: column;
    gap: 2px;
    min-width: 0;
  }

  .dns__summary-label {
    font-size: 11px;
    color: var(--color-text-muted, #6b7280);
  }

  .dns__summary-item strong {
    font-size: 14px;
    color: var(--color-text);
  }

  .dns__summary-hint {
    font-size: 11px;
    color: var(--color-text-muted, #6b7280);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .dns__dnssec--success {
    color: #16a34a;
  }

  .dns__dnssec--warning {
    color: #d97706;
  }

  .dns__dnssec--info {
    color: var(--color-text-muted, #6b7280);
  }

  /* Tab 切换 */
  .dns__tabs {
    display: flex;
    align-items: center;
    gap: 2px;
    border-bottom: 1px solid var(--color-border);
    padding: 0 4px;
  }

  .dns__tab {
    padding: 8px 14px;
    border: none;
    background: none;
    color: var(--color-text-muted, #6b7280);
    font-size: 13px;
    cursor: pointer;
    border-bottom: 2px solid transparent;
    transition: all 0.15s;
  }

  .dns__tab:hover {
    color: var(--color-text);
  }

  .dns__tab--active {
    color: var(--color-accent, #2563eb);
    border-bottom-color: var(--color-accent, #2563eb);
    font-weight: 600;
  }

  .dns__copy {
    margin-left: auto;
    padding: 4px 12px;
    border: 1px solid var(--color-border);
    border-radius: 4px;
    background: var(--color-bg);
    color: var(--color-text);
    font-size: 12px;
    cursor: pointer;
    transition: all 0.15s;
  }

  .dns__copy:hover {
    border-color: var(--color-accent, #2563eb);
    color: var(--color-accent, #2563eb);
  }

  .dns__tab-body {
    padding: 12px 0;
  }

  .dns__no-records {
    padding: 24px 8px;
    text-align: center;
    color: var(--color-text-muted, #6b7280);
  }

  /* 记录区段 */
  .dns__section {
    margin-bottom: 16px;
  }

  .dns__section:last-child {
    margin-bottom: 0;
  }

  .dns__section h3 {
    margin: 0 0 8px;
    font-size: 13px;
    font-weight: 600;
    color: var(--color-text);
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .dns__count {
    display: inline-block;
    padding: 1px 6px;
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.05));
    border-radius: 8px;
    font-size: 11px;
    color: var(--color-text-muted, #6b7280);
    font-weight: 500;
  }

  .dns__records {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .dns__record {
    padding: 10px 12px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm, 6px);
    background: var(--color-bg);
  }

  .dns__record-head {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 6px;
    flex-wrap: wrap;
  }

  .dns__record-name {
    font-family: ui-monospace, 'SF Mono', Consolas, monospace;
    font-size: 12px;
    color: var(--color-text);
    font-weight: 600;
    word-break: break-all;
  }

  .dns__record-type {
    display: inline-block;
    padding: 1px 6px;
    background: var(--color-accent, #2563eb);
    color: white;
    border-radius: 3px;
    font-size: 10px;
    font-weight: 600;
    font-family: ui-monospace, 'SF Mono', Consolas, monospace;
  }

  .dns__record-ttl {
    font-size: 11px;
    color: var(--color-text-muted, #6b7280);
    font-family: ui-monospace, 'SF Mono', Consolas, monospace;
  }

  .dns__record-label {
    display: inline-block;
    padding: 1px 6px;
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.05));
    border-radius: 3px;
    font-size: 10px;
    color: var(--color-text-muted, #6b7280);
  }

  .dns__record-data {
    font-size: 12px;
    line-height: 1.6;
    word-break: break-all;
  }

  .dns__record-formatted {
    padding: 6px 8px;
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.03));
    border-radius: 4px;
    color: var(--color-text);
    font-family: ui-monospace, 'SF Mono', Consolas, monospace;
    font-size: 12px;
  }

  .dns__record-raw {
    margin-top: 4px;
    font-size: 11px;
    color: var(--color-text-muted, #6b7280);
  }

  .dns__record-raw-label {
    color: var(--color-text-muted, #6b7280);
  }

  .dns__record-raw code,
  .dns__record-code {
    font-family: ui-monospace, 'SF Mono', Consolas, monospace;
    word-break: break-all;
  }

  .dns__dig,
  .dns__raw {
    margin: 0;
    padding: 12px;
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.03));
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm, 6px);
    font-family: ui-monospace, 'SF Mono', Consolas, monospace;
    font-size: 12px;
    line-height: 1.5;
    overflow-x: auto;
    color: var(--color-text);
    white-space: pre-wrap;
    word-break: break-all;
  }

  /* 响应式：768px 单列堆叠 */
  @media (max-width: 768px) {
    .dns__main {
      grid-template-columns: 1fr;
    }

    .dns__summary {
      grid-template-columns: 1fr;
      gap: 6px;
    }

    .dns__summary-hint {
      white-space: normal;
    }
  }

  /* 响应式：414px 紧凑布局 */
  @media (max-width: 414px) {
    .dns__container {
      margin: 16px 0;
    }

    .dns__panel,
    .dns__result {
      padding: 12px;
    }

    .dns__presets {
      padding: 10px;
    }

    .dns__record {
      padding: 8px 10px;
    }

    .dns__tabs {
      flex-wrap: wrap;
    }

    .dns__copy {
      margin-left: 0;
      margin-top: 4px;
    }
  }

  /* 暗色模式适配 */
  :global(.dark) .dns__record-type {
    color: #fff;
  }

  :global(.dark) .dns__dnssec--success {
    color: #4ade80;
  }

  :global(.dark) .dns__dnssec--warning {
    color: #fbbf24;
  }
</style>
