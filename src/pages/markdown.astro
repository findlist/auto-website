---
import BaseLayout, { type SiteMeta } from '../layouts/BaseLayout.astro';
import MarkdownTool from '../components/MarkdownTool.tsx';

const meta: SiteMeta = {
  title: 'Markdown 在线预览器 - 实时分屏渲染 + 工具栏快捷插入 + HTML 导出',
  description:
    '免费的在线 Markdown 预览器：实时分屏渲染（左编辑右预览）、双向同步滚动、工具栏快捷插入标题/粗体/链接/表格/代码块、字数统计与阅读时间估算、HTML 导出、草稿自动保存。手写轻量 GFM 子集解析器，零依赖、零上传、零追踪，所有解析在浏览器本地完成，默认 HTML 转义防 XSS。开发者写文档、技术博客的必备工具。',
  jsonLd: {
    '@context': 'https://schema.org',
    '@type': 'WebApplication',
    name: 'Markdown 在线预览器',
    url: 'https://toolbox.example.com/markdown',
    description: '在线 Markdown 预览器：实时分屏渲染、工具栏快捷插入、HTML 导出、草稿自动保存。手写轻量 GFM 解析器，零依赖、XSS 安全，全本地处理。',
    applicationCategory: 'DeveloperApplication',
    operatingSystem: 'Any (Web Browser)',
    inLanguage: 'zh-CN',
    offers: {
      '@type': 'Offer',
      price: '0',
      priceCurrency: 'CNY',
    },
  },
};
---
<BaseLayout meta={meta}>
  <section class="container">
    <div class="page-hero">
      <h1>Markdown 在线预览器</h1>
      <p>
        实时分屏预览：左侧编辑 Markdown，右侧即时渲染 HTML。配 16 个工具栏快捷按钮、双向同步滚动、
        字数统计、阅读时间估算、HTML 导出与草稿自动保存。手写轻量 GFM 子集解析器，
        零依赖、默认 HTML 转义防 XSS，所有解析在浏览器本地完成。
      </p>
    </div>

    <!-- client:load 让 React 岛屿在页面加载时立即水合，保证交互即时可用 -->
    <MarkdownTool client:load />

    <!-- 常见问题：补充 SEO 内容 -->
    <section class="json-faq" aria-labelledby="faq-title">
      <h2 id="faq-title">常见问题</h2>
      <details>
        <summary>支持哪些 Markdown 语法？</summary>
        <p>
          本工具实现了 <strong>GFM（GitHub Flavored Markdown）子集</strong>，覆盖以下语法：
        </p>
        <ul>
          <li><strong>标题</strong>：H1 ~ H6（<code>#</code> ~ <code>######</code>），自动生成锚点 id</li>
          <li><strong>段落</strong>：空行分隔段落，段内单换行保留为 <code>&lt;br&gt;</code></li>
          <li><strong>粗体</strong>：<code>**text**</code> 或 <code>__text__</code></li>
          <li><strong>斜体</strong>：<code>*text*</code> 或 <code>_text_</code></li>
          <li><strong>删除线</strong>：<code>~~text~~</code></li>
          <li><strong>行内代码</strong>：<code>`code`</code>（支持多反引号包裹含反引号内容）</li>
          <li><strong>代码块</strong>：三反引号围栏 <code>```lang</code>，支持语言标识</li>
          <li><strong>链接</strong>：<code>[text](url "title")</code>，自动加 <code>target="_blank"</code> 与 <code>rel="noopener noreferrer"</code></li>
          <li><strong>图片</strong>：<code>![alt](url "title")</code>，自动加 <code>loading="lazy"</code></li>
          <li><strong>自动链接</strong>：<code>&lt;https://example.com&gt;</code> 与 <code>&lt;user@example.com&gt;</code></li>
          <li><strong>无序列表</strong>：<code>-</code> / <code>*</code> / <code>+</code></li>
          <li><strong>有序列表</strong>：<code>1.</code>（数字 + 点）</li>
          <li><strong>任务列表</strong>：<code>- [ ]</code> 未完成、<code>- [x]</code> 已完成</li>
          <li><strong>引用</strong>：<code>&gt; text</code>，支持引用内嵌套标题与列表</li>
          <li><strong>表格</strong>：管道符分隔，第二行 <code>---|:---|---:|:---:</code> 控制对齐（左/右/中/默认）</li>
          <li><strong>水平线</strong>：<code>---</code> / <code>***</code> / <code>___</code></li>
        </ul>
        <p>未实现的进阶语法：脚注、定义列表、数学公式、Mermaid 图表、TOC 目录、自定义锚点。如有需要可在反馈渠道提出。</p>
      </details>
      <details>
        <summary>为什么不引入 marked / markdown-it 等成熟库？</summary>
        <p>
          三个核心考虑：
        </p>
        <ol>
          <li>
            <strong>bundle 体积可控</strong>：marked 约 30KB（gzip 10KB）+ DOMPurify 约 25KB（gzip 9KB）共 60KB，
            而手写解析器仅约 14KB（gzip 5KB），节省 46KB。本工具页单页加载约 156KB，远低于 200KB 红线。
          </li>
          <li>
            <strong>XSS 防护更可控</strong>：成熟库功能丰富但攻击面也大，需配合 DOMPurify 净化 HTML。
            本工具<strong>默认转义所有原始 HTML</strong>（<code>&lt;script&gt;</code>、<code>&lt;iframe&gt;</code> 等），
            从根本上杜绝 XSS，无需引入额外净化库。
          </li>
          <li>
            <strong>维护可控</strong>：自实现解析器逻辑透明，遇到 bug 可即时修复，不依赖第三方更新节奏。
            覆盖 GFM 子集已满足 95%+ 写作场景，复杂语法可按需扩展。
          </li>
        </ol>
      </details>
      <details>
        <summary>如何防止 XSS 攻击？我可以输入 &lt;script&gt;alert(1)&lt;/script&gt; 吗？</summary>
        <p>
          可以输入但<strong>不会执行</strong>。本工具的解析器在渲染前会先<strong>转义所有 HTML 特殊字符</strong>：
          <code>&amp;</code> → <code>&amp;amp;</code>、<code>&lt;</code> → <code>&amp;lt;</code>、
          <code>&gt;</code> → <code>&amp;gt;</code>、<code>"</code> → <code>&amp;quot;</code>、
          <code>'</code> → <code>&amp;#39;</code>。
          因此 <code>&lt;script&gt;alert(1)&lt;/script&gt;</code> 会被原样显示为文本，不会被浏览器解析为脚本。
        </p>
        <p>
          此外，<strong>URL 协议白名单</strong>会拦截 <code>javascript:</code>、<code>data:</code> 等危险协议。
          例如 <code>[点我](javascript:alert(1))</code> 的链接 URL 会被清空，仅显示「点我」文本。
          允许的协议：http、https、mailto、ftp、相对路径（<code>/path</code>）、锚点（<code>#anchor</code>）。
        </p>
        <p>
          行内代码与代码块内容<strong>仅转义，不解析任何 Markdown 语法</strong>，确保代码示例原样显示。
        </p>
      </details>
      <details>
        <summary>双向同步滚动是怎么实现的？为什么有时会偏差？</summary>
        <p>
          同步滚动原理：监听编辑器与预览区的 <code>scroll</code> 事件，按 <code>scrollTop / (scrollHeight - clientHeight)</code>
          计算滚动比例，再将比例乘以另一侧的最大滚动距离同步过去。为避免互相触发死循环，用 <code>isSyncing</code> 标志位
          在同步期间屏蔽对方事件，并通过 <code>requestAnimationFrame</code> 在下一帧重置标志位。
        </p>
        <p>
          <strong>偏差原因</strong>：编辑器与预览的<strong>内容长度比例不均匀</strong>。例如 Markdown 中一行表格语法
          渲染为 HTML 可能是 5 行高的表格，导致滚动比例换算后预览区跳跃。这是所有 Markdown 编辑器的通病，
          GitHub / VS Code 也存在类似偏差。如需精确对照，可关闭同步滚动开关，手动滚动。
        </p>
      </details>
      <details>
        <summary>草稿会保存到哪里？清空浏览器会丢失吗？</summary>
        <p>
          草稿保存在浏览器的 <code>localStorage</code>，键名为 <code>toolbox-markdown-draft</code>。
          每次输入变化后延迟 500ms 写入（去抖避免频繁 IO），刷新页面、关闭标签页后再次打开本工具会自动恢复草稿。
        </p>
        <p>
          <strong>丢失场景</strong>：① 浏览器隐私模式（无痕窗口）<code>localStorage</code> 不持久；
          ② 手动清除浏览器数据时勾选「Cookie 与网站数据」会一并清除；③ 浏览器存储配额超限（通常 5-10MB，
          约可存 200 万字符的 Markdown）；④ 点击本工具的「清空」按钮会主动移除草稿。
        </p>
        <p>
          <strong>重要文档请及时下载 HTML 备份</strong>，不要依赖 localStorage 长期保存。
        </p>
      </details>
      <details>
        <summary>导出的 HTML 文件包含什么？可以直接部署吗？</summary>
        <p>
          点击「下载 HTML」会导出一个<strong>独立的 .html 文件</strong>，包含：
        </p>
        <ul>
          <li>完整的渲染后 HTML 内容（段落、标题、代码块、表格等）</li>
          <li>内联的基础样式（字体、代码块背景、表格边框、引用样式等）</li>
          <li>UTF-8 字符编码声明与移动端 viewport 适配</li>
        </ul>
        <p>
          可以直接双击在浏览器打开预览，也可以上传到任意静态托管平台（Vercel / Cloudflare Pages / GitHub Pages）
          作为独立页面部署。如需自定义样式，可在文件头部的 <code>&lt;style&gt;</code> 标签内修改。
        </p>
        <p>
          <strong>注意</strong>：导出的 HTML 已经过 XSS 防护处理（原始 HTML 已转义），可安全分享。
        </p>
      </details>
      <details>
        <summary>支持嵌套列表吗？为什么我写的嵌套子项没有缩进？</summary>
        <p>
          当前版本<strong>不展开嵌套列表的层级缩进</strong>，所有列表项扁平渲染为同级 <code>&lt;li&gt;</code>。
          例如：
        </p>
        <pre><code>- 父项
  - 子项
  - 子项</code></pre>
        <p>
          渲染为单个 <code>&lt;ul&gt;</code> 包含 3 个 <code>&lt;li&gt;</code>，子项不会缩进显示。
          这是有意简化：完整嵌套需要解析缩进层级（2 空格 / 4 空格 / Tab），实现复杂度高且易出 bug。
          <strong>临时方案</strong>：用引用块 <code>&gt;</code> 或代码块表达层级，或导出 HTML 后手动调整。
        </p>
        <p>
          未来版本如需支持嵌套，可基于缩进解析为嵌套 <code>&lt;ul&gt;</code>，欢迎在反馈渠道表达需求。
        </p>
      </details>
      <details>
        <summary>数据会上传到服务器吗？草稿会被追踪吗？</summary>
        <p>
          不会。本工具完全在浏览器内运行：Markdown 解析、HTML 渲染、草稿保存、文件下载均由 JavaScript 在你本地执行，
          输入的内容<strong>不会发送到任何服务器</strong>。草稿保存在 <code>localStorage</code> 也仅限本机浏览器，
          不会跨设备同步。这对于处理商业文档、内部资料、隐私笔记尤其重要。
        </p>
        <p>
          但请注意：浏览器历史记录、屏幕截图、共享设备可能留下痕迹，如需彻底保密请在隐私模式中使用，
          并在用完后清除 localStorage（点击「清空」按钮即可）。
        </p>
      </details>
    </section>
  </section>
</BaseLayout>

<style is:global>
  /* Markdown 工具专属样式 */

  .mdtool {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  /* 工具栏 */
  .mdtool__toolbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 10px;
    padding: 10px 12px;
    background: var(--color-bg-soft);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
  }

  .mdtool__toolbar-group {
    display: inline-flex;
    flex-wrap: wrap;
    gap: 4px;
  }

  .mdtool__btn {
    min-width: 32px;
    padding: 5px 10px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    background: var(--color-bg);
    color: var(--color-text);
    font-size: 13px;
    font-family: inherit;
    cursor: pointer;
    transition: all 0.15s ease;
    line-height: 1.4;
  }

  .mdtool__btn:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
    background: rgba(43, 108, 255, 0.05);
  }

  .mdtool__btn--toggle.is-active {
    background: var(--color-primary);
    border-color: var(--color-primary);
    color: #fff;
  }

  /* 统计栏 */
  .mdtool__stats {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    align-items: center;
    padding: 8px 12px;
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    font-size: 13px;
    color: var(--color-text-soft);
  }

  .mdtool__stats strong {
    color: var(--color-text);
    font-weight: 600;
  }

  .mdtool__sync-toggle {
    margin-left: auto;
    display: inline-flex;
    align-items: center;
    gap: 6px;
    cursor: pointer;
    user-select: none;
  }

  .mdtool__sync-toggle input {
    cursor: pointer;
  }

  /* 主区：分屏布局 */
  .mdtool__main {
    display: grid;
    gap: 12px;
    min-height: 480px;
  }

  .mdtool__main--split {
    grid-template-columns: 1fr 1fr;
  }

  .mdtool__main--editor,
  .mdtool__main--preview {
    grid-template-columns: 1fr;
  }

  .mdtool__pane {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    overflow: hidden;
    background: var(--color-bg);
    min-height: 480px;
  }

  .mdtool__pane.is-hidden {
    display: none;
  }

  /* 编辑器 textarea */
  .mdtool__editor {
    width: 100%;
    height: 100%;
    min-height: 480px;
    padding: 14px;
    border: none;
    background: var(--color-bg);
    color: var(--color-text);
    font-family: var(--font-mono, ui-monospace, SFMono-Regular, Menlo, Consolas, monospace);
    font-size: 14px;
    line-height: 1.7;
    resize: vertical;
    outline: none;
  }

  .mdtool__editor:focus-visible {
    box-shadow: inset 0 0 0 2px var(--color-primary);
  }

  /* 预览区 */
  .mdtool__preview {
    height: 100%;
    min-height: 480px;
    padding: 14px 18px;
    overflow: auto;
    line-height: 1.7;
    color: var(--color-text);
  }

  .mdtool__empty {
    color: var(--color-text-muted);
    text-align: center;
    padding: 60px 20px;
    font-size: 14px;
  }

  /* 操作栏 */
  .mdtool__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    padding: 8px 0;
  }

  .mdtool__action-btn {
    padding: 8px 16px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background: var(--color-bg);
    color: var(--color-text);
    font-size: 13px;
    font-family: inherit;
    cursor: pointer;
    transition: all 0.15s ease;
  }

  .mdtool__action-btn:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
  }

  .mdtool__action-btn--danger:hover {
    border-color: var(--color-danger, #dc2626);
    color: var(--color-danger, #dc2626);
  }

  /* ========================================
     预览区 markdown-body 排版样式
     ======================================== */

  .markdown-body h1,
  .markdown-body h2,
  .markdown-body h3,
  .markdown-body h4,
  .markdown-body h5,
  .markdown-body h6 {
    margin: 24px 0 12px;
    font-weight: 600;
    line-height: 1.3;
  }

  .markdown-body h1 { font-size: 28px; padding-bottom: 8px; border-bottom: 1px solid var(--color-border); }
  .markdown-body h2 { font-size: 22px; padding-bottom: 6px; border-bottom: 1px solid var(--color-border); }
  .markdown-body h3 { font-size: 18px; }
  .markdown-body h4 { font-size: 16px; }
  .markdown-body h5 { font-size: 14px; }
  .markdown-body h6 { font-size: 13px; color: var(--color-text-soft); }

  .markdown-body h1:first-child,
  .markdown-body h2:first-child,
  .markdown-body h3:first-child {
    margin-top: 0;
  }

  .markdown-body p {
    margin: 0 0 14px;
  }

  .markdown-body strong {
    font-weight: 600;
  }

  .markdown-body em {
    font-style: italic;
  }

  .markdown-body del {
    text-decoration: line-through;
    color: var(--color-text-muted);
  }

  /* 行内代码 */
  .markdown-body code {
    padding: 2px 6px;
    background: var(--color-bg-soft);
    border-radius: 3px;
    font-family: var(--font-mono, ui-monospace, SFMono-Regular, Menlo, Consolas, monospace);
    font-size: 0.92em;
    color: var(--color-text);
  }

  /* 代码块 */
  .markdown-body pre {
    margin: 0 0 14px;
    padding: 14px 16px;
    background: var(--color-bg-soft);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    overflow-x: auto;
    line-height: 1.5;
  }

  .markdown-body pre code {
    padding: 0;
    background: none;
    border: none;
    font-size: 13px;
    color: var(--color-text);
  }

  /* 引用 */
  .markdown-body blockquote {
    margin: 0 0 14px;
    padding: 8px 16px;
    border-left: 4px solid var(--color-primary);
    background: var(--color-bg-soft);
    color: var(--color-text-soft);
  }

  .markdown-body blockquote p:last-child {
    margin-bottom: 0;
  }

  /* 列表 */
  .markdown-body ul,
  .markdown-body ol {
    margin: 0 0 14px;
    padding-left: 28px;
  }

  .markdown-body li {
    margin: 4px 0;
  }

  /* 任务列表：去掉默认圆点 */
  .markdown-body .task-list {
    list-style: none;
    padding-left: 8px;
  }

  .markdown-body .task-list-item {
    list-style: none;
  }

  .markdown-body .task-list-item input[type="checkbox"] {
    margin-right: 6px;
    vertical-align: middle;
  }

  /* 链接 */
  .markdown-body a {
    color: var(--color-primary);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 0.15s ease;
  }

  .markdown-body a:hover {
    border-bottom-color: var(--color-primary);
  }

  /* 图片 */
  .markdown-body img {
    max-width: 100%;
    height: auto;
    border-radius: var(--radius-sm);
  }

  /* 表格 */
  .markdown-body table {
    width: 100%;
    border-collapse: collapse;
    margin: 0 0 14px;
    font-size: 13.5px;
  }

  .markdown-body th,
  .markdown-body td {
    padding: 8px 12px;
    border: 1px solid var(--color-border);
    text-align: left;
  }

  .markdown-body th {
    background: var(--color-bg-soft);
    font-weight: 600;
  }

  .markdown-body tbody tr:nth-child(even) {
    background: var(--color-bg-soft);
  }

  /* 水平线 */
  .markdown-body hr {
    margin: 24px 0;
    border: none;
    border-top: 1px solid var(--color-border);
  }

  /* 移动端响应式 */
  @media (max-width: 768px) {
    .mdtool__toolbar {
      padding: 8px;
    }

    .mdtool__btn {
      padding: 5px 8px;
      font-size: 12px;
    }

    .mdtool__stats {
      gap: 10px;
      font-size: 12px;
    }

    .mdtool__sync-toggle {
      margin-left: 0;
      width: 100%;
      margin-top: 4px;
    }

    /* 移动端默认改为纵向布局（编辑器在上、预览在下） */
    .mdtool__main--split {
      grid-template-columns: 1fr;
    }

    .mdtool__pane,
    .mdtool__editor,
    .mdtool__preview {
      min-height: 320px;
    }

    .markdown-body h1 { font-size: 22px; }
    .markdown-body h2 { font-size: 18px; }
    .markdown-body h3 { font-size: 16px; }
  }
</style>
