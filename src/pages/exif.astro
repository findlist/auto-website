---
import BaseLayout, { type SiteMeta } from '../layouts/BaseLayout.astro';
import ExifTool from '../components/ExifTool.tsx';

const meta: SiteMeta = {
  title: 'EXIF 信息查看器 - 在线图片元数据解析工具',
  description:
    '在线 EXIF 信息查看器：解析 JPEG/PNG/WebP/TIFF/HEIC 图片元数据，查看相机型号、拍摄参数、GPS 定位与拍摄时间，一键复制 JSON。全本地处理，零广告零追踪。',
  jsonLd: {
    '@context': 'https://schema.org',
    '@type': 'WebApplication',
    name: 'EXIF 信息查看器',
    description: '在线图片 EXIF 元数据解析工具，支持 JPEG/PNG/WebP/TIFF/HEIC 格式，全本地解析，查看相机、拍摄参数、GPS 定位等信息。',
    applicationCategory: 'DeveloperApplication',
    operatingSystem: 'Any (Web Browser)',
    inLanguage: 'zh-CN',
    offers: {
      '@type': 'Offer',
      price: '0',
      priceCurrency: 'CNY',
    },
  },
};
---
<BaseLayout meta={meta}>
  <section class="container">
    <div class="page-hero page-hero--tool">
      <h1>EXIF 信息查看器</h1>
      <p>
        支持 JPEG / PNG / WebP / TIFF / HEIC 等格式的 EXIF / IPTC / XMP / ICC 元数据解析。
        查看相机型号、拍摄参数（光圈、快门、ISO、焦距）、GPS 定位、拍摄时间等完整信息。
        基于 exifr 库在浏览器本地完成解析，零上传、零追踪。适用于照片信息查看、拍摄参数分析、隐私元数据检查等场景。
      </p>
    </div>

    <!-- client:load 让 React 岛屿在页面加载时立即水合，保证拖拽与粘贴交互即时可用 -->
    <ExifTool client:load />

    <section class="json-faq" aria-labelledby="faq-title">
      <h2 id="faq-title">常见问题</h2>
      <details>
        <summary>什么是 EXIF 元数据？</summary>
        <p>
          <strong>EXIF</strong>（Exchangeable Image File Format，可交换图像文件格式）是嵌入在图片文件中的元数据标准，
          记录了照片的拍摄信息，包括相机制造商与型号、镜头、光圈（f/值）、快门速度、ISO 感光度、焦距、
          曝光补偿、闪光灯状态、白平衡、GPS 经纬度坐标、拍摄时间等。这些信息由相机或手机在拍照时自动写入，
          嵌入在 JPEG/TIFF 文件的 APP1 段中。EXIF 元数据对摄影分析、后期处理参考、拍摄位置回溯有重要价值。
        </p>
      </details>
      <details>
        <summary>支持哪些图片格式和元数据标准？</summary>
        <p>
          本工具基于 <strong>exifr</strong> 库，支持以下格式的元数据解析：
          <strong>JPEG</strong>（最常见，EXIF 数据最完整）、<strong>PNG</strong>（PNG 专有的 IHDR 块）、
          <strong>WebP</strong>、<strong>TIFF</strong>（原始 EXIF 格式）、<strong>HEIC/HEIF</strong>（苹果设备默认格式）。
          元数据标准覆盖：<strong>EXIF</strong>（拍摄参数）、<strong>IPTC</strong>（新闻通信元数据，含版权、关键词）、
          <strong>XMP</strong>（Adobe 可扩展元数据平台）、<strong>ICC Profile</strong>（色彩管理配置）、
          <strong>JFIF</strong>（JPEG 文件交换格式标识）。
        </p>
      </details>
      <details>
        <summary>为什么有些图片看不到 EXIF 信息？</summary>
        <p>
          常见原因有四种：① <strong>社交平台压缩</strong>：微信、微博、QQ 等平台在发送图片时会默认压缩并剥离元数据，
          以保护用户隐私和减小体积；② <strong>导出时剥离</strong>：部分图片处理软件（如 Photoshop「导出为 Web 所用格式」）
          默认移除 EXIF；③ <strong>纯绘图软件生成</strong>：由代码绘制或矢量软件导出的图片不含相机拍摄信息；
          ④ <strong>格式不支持</strong>：GIF、SVG 等格式不嵌入 EXIF 数据。如需查看完整 EXIF，请使用原图（未经压缩处理的原始文件）。
        </p>
      </details>
      <details>
        <summary>GPS 定位信息是如何工作的？</summary>
        <p>
          支持 GPS 的相机或手机在拍照时会记录拍摄位置的<strong>经纬度坐标</strong>（WGS-84 坐标系）和海拔高度。
          EXIF 中的 GPS 数据原始格式为度分秒（DMS）加方向参考（N/S/E/W），本工具通过 exifr 的 <code>reviveValues</code>
          自动将其转换为<strong>十进制度数</strong>（如 39.9042°），并提供 OpenStreetMap 地图链接，点击即可在地图中
          查看拍摄位置。注意：GPS 数据的精度取决于设备，室内拍照可能定位偏差较大。
        </p>
      </details>
      <details>
        <summary>快门速度为什么显示为 1/200 秒？</summary>
        <p>
          相机的快门速度（ExposureTime）在 EXIF 中以<strong>秒为单位</strong>存储（如 0.005 秒）。
          当快门速度小于 1 秒时，摄影领域习惯用<strong>分数形式</strong>表示（1/200 秒），更直观地反映曝光时长。
          本工具自动将小于 1 的快门速度转换为 1/x 分数格式；大于等于 1 秒的则直接显示为秒数（如 2 秒、30 秒），
          适用于长曝光场景。
        </p>
      </details>
      <details>
        <summary>如何清除图片的 EXIF 元数据保护隐私？</summary>
        <p>
          如需在分享图片前清除 EXIF 元数据，可以使用本站的<strong><a href="/image-compress">图片压缩工具</a></strong>——
          Canvas API 在重新编码时会丢弃原始 EXIF 数据。也可在系统自带的照片应用中关闭「位置信息」再分享，
          或使用专业的元数据编辑工具。注意：清除 EXIF 后拍摄参数、GPS 位置等信息将不可恢复，
          建议在分享前保留原图备份。
        </p>
      </details>
      <details>
        <summary>图片会上传到服务器吗？安全吗？</summary>
        <p>
          不会。本工具完全在浏览器内运行，所有 EXIF 解析由 exifr 库在本地 JavaScript 环境中执行，
          不会发送任何图片数据或元数据到服务器。使用 <code>URL.createObjectURL()</code> 创建本地预览 URL，
          图片文件始终保留在浏览器内存中。全程零网络请求，可离线使用，适合处理含敏感信息的照片（如含 GPS 定位的私人照片）。
        </p>
      </details>
      <details>
        <summary>分类视图和 JSON 原始数据视图有什么区别？</summary>
        <p>
          <strong>分类视图</strong>：将 EXIF 标签按图片属性、相机与镜头、拍摄参数、GPS 定位、时间信息、版权与描述
          六个分组展示，并对常见值做友好格式化（如快门速度显示为 1/200 秒、光圈显示为 f/2.8、GPS 显示为十进制度数），
          适合快速查看关键信息。
          <strong>JSON 原始数据视图</strong>：展示 exifr 解析后的原始 JSON 对象，包含所有标签的原始键名和值，
          适合开发者调试、数据迁移或二次开发。两种视图均支持搜索过滤，可一键复制完整 JSON。
        </p>
      </details>
    </section>

    <!-- 相关工具：图像处理矩阵内链，提升站内导航与 SEO 内链结构 -->
    <section class="related-tools" aria-labelledby="related-title">
      <h2 id="related-title">相关工具</h2>
      <ul class="related-tools__list">
        <li><a href="/exif-editor">EXIF 元数据编辑器</a> —— 删除 GPS / 个人信息 / 修改拍摄时间</li>
        <li><a href="/image-compress">图片压缩</a> —— 单文件位图压缩与质量调优</li>
        <li><a href="/image-convert">图片格式转换</a> —— PNG/JPEG/WebP/AVIF 批量互转</li>
        <li><a href="/image-resize">图片缩放</a> —— 5 种模式 + 8 种预设 + 等比锁定</li>
        <li><a href="/image-crop">图片裁剪</a> —— 可视化裁剪框 + 9 种比例 + 撤销重做</li>
        <li><a href="/image-watermark">图片水印</a> —— 文字/图片水印批量添加</li>
        <li><a href="/base64-image">Base64 图片互转</a> —— 图片与 data URL 互转</li>
        <li><a href="/svg-optimizer">SVG 优化器</a> —— 矢量图压缩与编辑器残留清理</li>
        <li><a href="/image-compare">图片对比</a> —— 左右并排 / 滑块叠加 / 像素差异高亮</li>
      </ul>
    </section>

    <!-- 相关博客：工具页反向内链，引导用户深度阅读（脚本批量生成，请勿手动修改） -->
    <section class="related-blogs" aria-labelledby="related-blogs-title">
      <h2 id="related-blogs-title">相关博客</h2>
      <ul class="related-blogs__list">
        <li>
          <a href="/blog/exif-metadata-guide">EXIF 元数据深度指南：图片信息解析、相机参数与隐私保护</a><span class="related-blogs__desc">系统讲解图片 EXIF 元数据的结构与解析原理：JPEG 文件格式、TIFF/IFD 数据结构、常见拍摄参数标签、GPS 坐标处理、exifr 库的浏览器端实现。帮助开发者理…</span>
        </li>
      </ul>
    </section>
  </section>
</BaseLayout>

<style is:global>
  /* EXIF 查看器专属样式 */
  .exif {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  /* 拖拽上传区 */
  .exif__dropzone {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 48px 24px;
    border: 2px dashed var(--color-border);
    border-radius: var(--radius-md);
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.03));
    cursor: pointer;
    transition: border-color var(--transition-fast, 0.15s), background var(--transition-fast, 0.15s);
    text-align: center;
  }

  .exif__dropzone:hover,
  .exif__dropzone--active {
    border-color: var(--color-primary, #2b6cff);
    background: var(--color-primary-soft, rgba(43, 108, 255, 0.06));
  }

  .exif__dropzone-icon {
    font-size: 48px;
    line-height: 1;
  }

  .exif__dropzone-text {
    font-size: 16px;
    font-weight: 600;
    color: var(--color-text);
  }

  .exif__dropzone-hint {
    font-size: 13px;
    color: var(--color-text-soft);
  }

  .exif__file-input {
    display: none;
  }

  /* 图片预览条 */
  .exif__preview-bar {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 16px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.03));
  }

  .exif__thumb {
    flex-shrink: 0;
    width: 72px;
    height: 72px;
    border-radius: var(--radius-sm, 6px);
    overflow: hidden;
    border: 1px solid var(--color-border);
    background: #fff;
  }

  .exif__thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .exif__file-info {
    flex: 1;
    min-width: 0;
  }

  .exif__file-name {
    font-weight: 600;
    font-size: 15px;
    color: var(--color-text);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .exif__file-meta {
    font-size: 13px;
    color: var(--color-text-soft);
    margin-top: 4px;
  }

  .exif__loading {
    font-size: 13px;
    color: var(--color-primary, #2b6cff);
    margin-top: 4px;
  }

  .exif__actions {
    display: flex;
    gap: 8px;
    flex-shrink: 0;
  }

  /* 按钮 */
  .exif__btn {
    padding: 8px 16px;
    font-size: 14px;
    font-weight: 500;
    border: 1px solid var(--color-primary, #2b6cff);
    border-radius: var(--radius-sm, 6px);
    background: var(--color-primary, #2b6cff);
    color: #fff;
    cursor: pointer;
    transition: opacity var(--transition-fast, 0.15s);
  }

  .exif__btn:hover {
    opacity: 0.85;
  }

  .exif__btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  .exif__btn--ghost {
    background: transparent;
    color: var(--color-primary, #2b6cff);
  }

  /* 工具栏 */
  .exif__toolbar {
    display: flex;
    align-items: center;
    gap: 12px;
    flex-wrap: wrap;
  }

  .exif__search {
    flex: 1;
    min-width: 200px;
    padding: 9px 14px;
    font-size: 14px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm, 6px);
    background: var(--color-bg, #fff);
    color: var(--color-text);
  }

  .exif__search:focus {
    outline: none;
    border-color: var(--color-primary, #2b6cff);
  }

  .exif__toolbar-btns {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
  }

  /* 错误提示 */
  .exif__error {
    padding: 12px 16px;
    border-radius: var(--radius-sm, 6px);
    background: var(--color-error-soft, rgba(220, 38, 38, 0.08));
    border: 1px solid var(--color-error-border, rgba(220, 38, 38, 0.3));
    color: var(--color-error, #dc2626);
    font-size: 14px;
  }

  /* 空状态 */
  .exif__empty {
    text-align: center;
    padding: 48px 24px;
    color: var(--color-text-soft);
  }

  .exif__empty-icon {
    font-size: 48px;
    margin-bottom: 12px;
  }

  .exif__empty-hint {
    font-size: 13px;
    margin-top: 8px;
    max-width: 480px;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.6;
  }

  /* GPS 地图卡片 */
  .exif__map-card {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
    padding: 14px 18px;
    border-radius: var(--radius-md);
    background: var(--color-primary-soft, rgba(43, 108, 255, 0.06));
    border: 1px solid var(--color-primary-border, rgba(43, 108, 255, 0.2));
    font-size: 14px;
    color: var(--color-text);
  }

  .exif__map-icon {
    font-size: 20px;
  }

  .exif__map-link {
    margin-left: auto;
    color: var(--color-primary, #2b6cff);
    text-decoration: none;
    font-weight: 500;
  }

  .exif__map-link:hover {
    text-decoration: underline;
  }

  /* 分组 */
  .exif__groups {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .exif__group {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .exif__group-title {
    margin: 0;
    padding: 12px 18px;
    font-size: 15px;
    font-weight: 600;
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.03));
    border-bottom: 1px solid var(--color-border);
    color: var(--color-text);
  }

  .exif__tag-list {
    margin: 0;
    padding: 0;
  }

  .exif__tag-row {
    display: flex;
    gap: 16px;
    padding: 10px 18px;
    border-bottom: 1px solid var(--color-border);
    font-size: 14px;
  }

  .exif__tag-row:last-child {
    border-bottom: none;
  }

  .exif__tag-row dt {
    flex: 0 0 160px;
    color: var(--color-text-soft);
    font-weight: 400;
  }

  .exif__tag-row dd {
    flex: 1;
    margin: 0;
    color: var(--color-text);
    word-break: break-word;
    font-variant-numeric: tabular-nums;
  }

  /* JSON 原始数据 */
  .exif__raw-json {
    margin: 0;
    padding: 18px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background: var(--color-bg-soft, rgba(0, 0, 0, 0.03));
    font-family: var(--font-mono, 'SF Mono', Monaco, Consolas, monospace);
    font-size: 13px;
    line-height: 1.6;
    overflow-x: auto;
    white-space: pre;
    color: var(--color-text);
  }

  /* 响应式：平板 768px */
  @media (max-width: 768px) {
    .exif__preview-bar {
      flex-wrap: wrap;
    }

    .exif__file-info {
      flex: 1 1 calc(100% - 88px);
    }

    .exif__actions {
      flex: 1 1 100%;
      justify-content: flex-end;
    }

    .exif__toolbar {
      flex-direction: column;
      align-items: stretch;
    }

    .exif__search {
      width: 100%;
    }

    .exif__toolbar-btns {
      justify-content: flex-start;
    }

    .exif__tag-row {
      flex-direction: column;
      gap: 4px;
    }

    .exif__tag-row dt {
      flex: none;
    }
  }

  /* 响应式：手机 414px */
  @media (max-width: 414px) {
    .exif__dropzone {
      padding: 32px 16px;
    }

    .exif__preview-bar {
      padding: 12px;
      gap: 12px;
    }

    .exif__thumb {
      width: 56px;
      height: 56px;
    }

    .exif__btn {
      padding: 7px 12px;
      font-size: 13px;
    }

    .exif__group-title {
      padding: 10px 14px;
      font-size: 14px;
    }

    .exif__tag-row {
      padding: 8px 14px;
    }
  }

  /* 暗色模式 */
  :global(.dark) .exif__thumb {
    background: #1a1a1a;
  }

  :global(.dark) .exif__raw-json {
    background: rgba(255, 255, 255, 0.04);
  }
</style>
