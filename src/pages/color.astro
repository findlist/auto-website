---
import BaseLayout, { type SiteMeta } from '../layouts/BaseLayout.astro';
import ColorTool from '../components/ColorTool.tsx';

const meta: SiteMeta = {
  title: '颜色格式转换工具 - HEX / RGB / HSL / HSV / CMYK 在线互转',
  description:
    '免费的在线颜色格式转换工具：HEX / RGB / HSL / HSV / CMYK 五种格式互转，内置颜色拾取器、大色块预览、明暗渐变、5 种和谐配色方案（互补色 / 类似色 / 三角色 / 分割互补 / 四角色）。输入即识别，所有计算在浏览器本地处理，零上传、零追踪、零广告。',
  jsonLd: {
    '@context': 'https://schema.org',
    '@type': 'WebApplication',
    name: '颜色格式转换工具',
    description: '在线颜色格式转换工具：HEX / RGB / HSL / HSV / CMYK 互转，含配色方案生成，全本地处理。',
    applicationCategory: 'DesignApplication',
    operatingSystem: 'Any (Web Browser)',
    inLanguage: 'zh-CN',
    offers: {
      '@type': 'Offer',
      price: '0',
      priceCurrency: 'CNY',
    },
  },
};
---
<BaseLayout meta={meta}>
  <section class="container">
    <div class="page-hero">
      <h1>颜色格式转换工具</h1>
      <p>
        粘贴任意格式的颜色值立即转换为 HEX / RGB / HSL / HSV / CMYK 五种格式，内置颜色拾取器与
        5 种和谐配色方案生成。所有计算在浏览器本地完成，无需上传。
      </p>
    </div>

    <!-- client:load 让 React 岛屿在页面加载时立即水合，保证交互即时可用 -->
    <ColorTool client:load />

    <!-- 常见问题：补充 SEO 内容 -->
    <section class="json-faq" aria-labelledby="faq-title">
      <h2 id="faq-title">常见问题</h2>
      <details>
        <summary>HEX、RGB、HSL、HSV、CMYK 各是什么？分别用在什么场景？</summary>
        <p>
          <strong>HEX</strong>：十六进制颜色表示，网页最常用，如 <code>#2b6cff</code>，每两位表示红绿蓝（0-FF）；
          <strong>RGB</strong>：红绿蓝加色模型，三值各 0-255，CSS 与设计软件通用；
          <strong>HSL</strong>：色相（Hue，0-360°）/ 饱和度（Saturation，0-100%）/ 亮度（Lightness，0-100%），
          人眼直觉接近「这是什么颜色 / 多鲜艳 / 多亮」，CSS 标准支持；
          <strong>HSV</strong>：与 HSL 类似但 V（Value，明度）≠ L（亮度），Photoshop 取色器使用 HSV；
          <strong>CMYK</strong>：青 / 品红 / 黄 / 黑，印刷四色，对应油墨比例，与屏幕 RGB 加色模型相反。
        </p>
      </details>
      <details>
        <summary>HSL 和 HSV 有什么区别？为什么 Photoshop 显示 HSV 但 CSS 用 HSL？</summary>
        <p>
          两者都基于色相（H）+ 饱和度（S），但第三维不同：
          <strong>HSL 的 L（亮度）</strong>：0% 是纯黑，100% 是纯白，50% 是纯色；
          <strong>HSV 的 V（明度）</strong>：0% 是纯黑，100% 是纯色（不是白色）。
          关键差异：在 HSL 中纯白与纯色明度差 50%，在 HSV 中纯白 V=100 但饱和度 S=0，纯色 V=100、S=100。
          因此 <strong>纯色 → 加白</strong>在 HSL 中是 L↑，在 HSV 中是 S↓。
          Photoshop 取色器用 HSV 因为它对应画家「选色 → 加白变亮」的直觉；CSS 用 HSL 因为它对网页设计
          「明度均匀」更友好（同样的 L 值，不同色相看起来亮度一致）。
        </p>
      </details>
      <details>
        <summary>RGB 与 CMYK 为什么不能完全互转？</summary>
        <p>
          RGB 是<strong>加色模型</strong>（光叠加，三色相加为白），色域较大；CMYK 是<strong>减色模型</strong>
          （油墨吸收光，三色相加为黑），色域较小。许多亮色（如纯蓝 <code>rgb(0, 0, 255)</code>）
          在印刷中无法精确呈现，转换到 CMYK 会出现颜色失真（变暗变浊）。本工具的 RGB → CMYK 转换
          是数学上的等价转换，<strong>未做色域映射</strong>，实际印刷前应使用专业软件（如 Photoshop）
          做色彩管理与 ICC 配置文件转换。同理 CMYK → RGB 显示也只是近似值。
        </p>
      </details>
      <details>
        <summary>这个工具的配色方案是怎么生成的？</summary>
        <p>
          基于色彩理论的标准和谐方案，对当前颜色的 HSL 色相做旋转：
          <strong>互补色</strong>（±180°，2 色）对比强烈，适合强调；
          <strong>类似色</strong>（±30°，3 色）邻近色和谐柔顺，适合统一氛围；
          <strong>三角色</strong>（±120°，3 色）平衡活泼，适合多区域划分；
          <strong>分割互补</strong>（180°±30°，3 色）比互补色柔和，对比但不刺眼；
          <strong>四角色</strong>（±90°/±180°，4 色）丰富多样，适合多主题配色。
          所有方案保持当前颜色的饱和度与亮度，仅旋转色相。
        </p>
      </details>
      <details>
        <summary>支持哪些输入格式？要不要加前缀？</summary>
        <p>
          支持以下输入：
          <code>#rgb</code>、<code>#rrggbb</code>（带或不带 #）、
          <code>rgb(r, g, b)</code>、<code>hsl(h, s%, l%)</code>、<code>hsv(h, s%, v%)</code>、
          <code>cmyk(c%, m%, y%, k%)</code>。
          也可省略前缀只写数字：3 个数字（0-255）按 RGB 解析，4 个数字（0-100）按 CMYK 解析。
          建议加前缀以避免歧义，例如 <code>220, 100%, 58%</code> 可能是 HSL 也可能是 HSV，加上
          <code>hsl(...)</code> 才能确保正确。也可点击左侧色块用拾色器直接选色。
        </p>
      </details>
      <details>
        <summary>为什么有些颜色看起来和我设计软件里显示的不一样？</summary>
        <p>
          可能原因：1) <strong>色彩空间不同</strong>：本工具使用 sRGB（网页标准），设计软件可能用
          Display P3 或 Adobe RGB，色域更宽，亮色更鲜艳；2) <strong>浏览器未开启色彩管理</strong>：
          部分老浏览器不渲染 ICC 配置文件；3) <strong>HSL 与 HSV 混淆</strong>：Photoshop 取色器
          显示 HSV，但 CSS 用 HSL，数值相同但实际颜色不同（HSL 的 50% 亮度 ≠ HSV 的 100% 明度）。
          本工具明确标注每种的格式，避免混淆。
        </p>
      </details>
      <details>
        <summary>如何选择配色方案？</summary>
        <p>
          基础原则：<strong>互补色</strong>用于 CTA 按钮（高对比强调，如蓝色背景 + 橙色按钮）；
          <strong>类似色</strong>用于背景渐变或图表系列（和谐统一）；
          <strong>三角色</strong>用于品牌识别或三栏布局（活泼平衡）；
          <strong>分割互补</strong>用于需要对比但不想太刺眼的场景（如插画配色）；
          <strong>四角色</strong>用于需要丰富多样的配色（如数据可视化多系列）。
          选定主色后用本工具生成配色，再复制 HEX 到 CSS / Figma 即可。建议先选 1 个主色，
          再加 1 个强调色（互补或分割互补），最后用类似色或灰色填充背景。
        </p>
      </details>
      <details>
        <summary>数据会上传到服务器吗？</summary>
        <p>
          不会。本工具完全在浏览器内运行，所有颜色解析、格式转换、配色生成逻辑由 JavaScript 在你本地执行，
          输入的颜色值不会发送到任何服务器。这对于处理品牌色、商业设计稿配色尤其重要，
          可避免配色方案提前泄露。但请注意：浏览器历史记录、屏幕截图、共享设备可能留下痕迹，
          如需彻底保密请在隐私模式中使用。
        </p>
      </details>
    </section>
  </section>
</BaseLayout>

<style is:global>
  /* 颜色工具专属样式：复用 .jsontool 基础样式，仅补充差异部分 */

  /* 输入区头部：标签 + 操作按钮 */
  .colortool__input-area {
    padding: 16px;
    border-bottom: 1px solid var(--color-border);
  }

  .colortool__input-head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 8px;
    gap: 12px;
    flex-wrap: wrap;
  }

  .colortool__label {
    font-size: 13px;
    font-weight: 500;
    color: var(--color-text-soft);
  }

  /* 输入行：拾色器 + 文本输入框横排 */
  .colortool__input-row {
    display: flex;
    gap: 10px;
    align-items: stretch;
  }

  /* 拾色器：原生 input[type=color]，圆形美化 */
  .colortool__picker {
    width: 44px;
    height: 44px;
    min-width: 44px;
    padding: 0;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    background: var(--color-bg);
    cursor: pointer;
    overflow: hidden;
  }

  /* 不同浏览器拾色器内部样式统一 */
  .colortool__picker::-webkit-color-swatch-wrapper {
    padding: 4px;
  }
  .colortool__picker::-webkit-color-swatch {
    border: none;
    border-radius: 4px;
  }
  .colortool__picker::-moz-color-swatch {
    border: none;
    border-radius: 4px;
  }

  .colortool__input {
    flex: 1;
    min-width: 0;
    padding: 10px 12px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    background: var(--color-bg);
    color: var(--color-text);
    font-family: var(--font-mono, ui-monospace, SFMono-Regular, Menlo, monospace);
    font-size: 13px;
    line-height: 1.6;
    transition: border-color 0.15s ease, box-shadow 0.15s ease;
  }

  .colortool__input:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 0 3px rgba(43, 108, 255, 0.12);
  }

  .colortool__hint {
    margin: 6px 0 0;
    font-size: 12px;
    color: var(--color-text-muted);
  }

  /* 整体错误提示 */
  .colortool__error {
    margin: 12px 16px;
    padding: 10px 14px;
    border-radius: var(--radius-sm);
    background: rgba(220, 38, 38, 0.08);
    border: 1px solid rgba(220, 38, 38, 0.2);
    color: var(--color-error);
    font-size: 13px;
  }

  /* 预览行：大色块 + 渐变条 */
  .colortool__preview-row {
    display: flex;
    gap: 12px;
    padding: 16px;
    border-bottom: 1px solid var(--color-border);
    flex-wrap: wrap;
  }

  /* 大色块预览：背景为当前色，文字色按亮度自适应 */
  .colortool__preview {
    flex: 1;
    min-width: 200px;
    min-height: 120px;
    border-radius: var(--radius-md);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 6px;
    padding: 16px;
    border: 1px solid var(--color-border);
  }

  .colortool__preview-hex {
    font-family: var(--font-mono, ui-monospace, SFMono-Regular, Menlo, monospace);
    font-size: 22px;
    font-weight: 700;
    letter-spacing: 0.04em;
  }

  .colortool__preview-rgb {
    font-family: var(--font-mono, ui-monospace, SFMono-Regular, Menlo, monospace);
    font-size: 12.5px;
    opacity: 0.85;
  }

  /* 渐变预览：两个色条 */
  .colortool__gradients {
    display: flex;
    flex-direction: column;
    gap: 8px;
    min-width: 200px;
    flex: 1;
  }

  .colortool__gradient {
    flex: 1;
    min-height: 56px;
    border-radius: var(--radius-sm);
    border: 1px solid var(--color-border);
  }

  /* 五种格式输出列表 */
  .colortool__formats {
    list-style: none;
    margin: 0;
    padding: 0;
    border-bottom: 1px solid var(--color-border);
  }

  .colortool__format {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px 16px;
    border-bottom: 1px solid var(--color-border);
    flex-wrap: wrap;
  }

  .colortool__format:last-child {
    border-bottom: none;
  }

  .colortool__format-head {
    display: flex;
    flex-direction: column;
    gap: 2px;
    min-width: 120px;
    flex-shrink: 0;
  }

  .colortool__format-label {
    font-size: 13px;
    font-weight: 600;
    color: var(--color-text);
    font-family: var(--font-mono, ui-monospace, SFMono-Regular, Menlo, monospace);
  }

  .colortool__format-desc {
    font-size: 11.5px;
    color: var(--color-text-muted);
  }

  .colortool__format-value {
    flex: 1;
    min-width: 0;
    padding: 6px 10px;
    background: var(--color-bg-code);
    color: var(--color-text);
    font-family: var(--font-mono, ui-monospace, SFMono-Regular, Menlo, monospace);
    font-size: 12.5px;
    border-radius: var(--radius-sm);
    word-break: break-all;
  }

  .colortool__copy-btn {
    padding: 4px 12px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    background: var(--color-bg);
    color: var(--color-text-soft);
    font-size: 12px;
    font-family: inherit;
    cursor: pointer;
    transition: all 0.15s ease;
    flex-shrink: 0;
  }

  .colortool__copy-btn:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
  }

  /* 调色板区 */
  .colortool__palettes {
    padding: 16px;
  }

  .colortool__palettes-title {
    margin: 0 0 4px;
    font-size: 16px;
    font-weight: 600;
  }

  .colortool__palettes-desc {
    margin: 0 0 16px;
    font-size: 12.5px;
    color: var(--color-text-muted);
    line-height: 1.6;
  }

  .colortool__palette {
    margin-bottom: 16px;
  }

  .colortool__palette:last-child {
    margin-bottom: 0;
  }

  .colortool__palette-meta {
    display: flex;
    align-items: baseline;
    gap: 8px;
    margin-bottom: 8px;
    flex-wrap: wrap;
  }

  .colortool__palette-name {
    font-size: 13px;
    font-weight: 600;
    color: var(--color-text);
  }

  .colortool__palette-hint {
    font-size: 11.5px;
    color: var(--color-text-muted);
  }

  .colortool__palette-colors {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
    gap: 8px;
  }

  /* 单个调色板色块：点击设为当前色 */
  .colortool__palette-color {
    position: relative;
    min-height: 80px;
    border-radius: var(--radius-sm);
    border: 1px solid var(--color-border);
    cursor: pointer;
    overflow: hidden;
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    padding: 8px;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }

  .colortool__palette-color:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .colortool__palette-color:focus-visible {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
  }

  .colortool__palette-hex {
    font-family: var(--font-mono, ui-monospace, SFMono-Regular, Menlo, monospace);
    font-size: 11.5px;
    font-weight: 600;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
  }

  .colortool__palette-copy {
    padding: 2px 8px;
    border: 1px solid currentColor;
    border-radius: 999px;
    background: transparent;
    color: inherit;
    font-size: 10.5px;
    font-family: inherit;
    cursor: pointer;
    opacity: 0.85;
    transition: opacity 0.15s ease;
  }

  .colortool__palette-copy:hover {
    opacity: 1;
  }

  /* 状态提示 */
  .colortool__notice {
    margin: 12px 16px;
    padding: 8px 14px;
    border-radius: var(--radius-sm);
    background: rgba(26, 170, 85, 0.08);
    color: var(--color-success);
    font-size: 13px;
    text-align: center;
  }

  /* 移动端响应式 */
  @media (max-width: 768px) {
    .colortool__input-head {
      flex-direction: column;
      align-items: stretch;
    }
    .colortool__preview-row {
      flex-direction: column;
    }
    .colortool__gradients {
      flex-direction: row;
      min-width: 0;
    }
    .colortool__format {
      flex-direction: column;
      align-items: stretch;
    }
    .colortool__format-head {
      flex-direction: row;
      gap: 8px;
      min-width: 0;
    }
    .colortool__palette-colors {
      grid-template-columns: repeat(auto-fill, minmax(100px, 1fr));
    }
  }
</style>
