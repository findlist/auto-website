---
import BaseLayout, { type SiteMeta } from '../layouts/BaseLayout.astro';
import JsonToXmlTool from '../components/JsonToXmlTool.tsx';

const meta: SiteMeta = {
  title: 'JSON 转 XML 转换工具 - 在线 JSON 数据导出为 XML 文档',
  description:
    '在线 JSON 转 XML 转换器：支持根节点名、数组项名、属性风格、CDATA、缩进等配置，自动转义特殊字符与 well-formed 校验。全本地处理，零广告零追踪。',
  jsonLd: {
    '@context': 'https://schema.org',
    '@type': 'WebApplication',
    name: 'JSON 转 XML 转换工具',
    description: '在线 JSON 转 XML 转换器：可配置根节点名、数组项名、属性风格、CDATA、缩进，well-formed 校验，全本地处理。',
    applicationCategory: 'DeveloperApplication',
    operatingSystem: 'Any (Web Browser)',
    inLanguage: 'zh-CN',
    offers: {
      '@type': 'Offer',
      price: '0',
      priceCurrency: 'CNY',
    },
  },
};
---
<BaseLayout meta={meta}>
  <section class="container">
    <div class="page-hero page-hero--tool">
      <h1>JSON 转 XML 转换工具</h1>
      <p>
        将 JSON 数据一键转换为 XML 文档，支持根节点名、数组项名、属性风格、CDATA 包裹、XML 声明、缩进等配置，自动转义特殊字符，well-formed 校验。适用于 SOAP 服务对接、SVG 生成、Office 文档、Android 资源、旧系统配置等场景，所有数据在浏览器本地处理。
      </p>
    </div>

    <JsonToXmlTool client:load />

    <section class="json-faq" aria-labelledby="faq-title">
      <h2 id="faq-title">常见问题</h2>

      <details>
        <summary>1. JSON 转 XML 有哪些常见应用场景？</summary>
        <p>尽管 JSON 已成为现代 Web API 的事实标准，XML 在以下场景仍是必需格式：</p>
        <ul>
          <li><strong>SOAP Web Service</strong>：传统企业级服务（如银行、保险、SAP）的请求与响应均为 XML</li>
          <li><strong>SVG 图形</strong>：可缩放矢量图形本质是 XML，从 JSON 生成 SVG 是常见需求</li>
          <li><strong>Office 文档</strong>：docx / xlsx / pptx 内部均为 XML（Office Open XML 标准）</li>
          <li><strong>Android 资源</strong>：布局文件 strings.xml / styles.xml / layout 均为 XML</li>
          <li><strong>配置文件</strong>：Java Spring、.NET WebService、Maven pom.xml 等旧系统配置</li>
          <li><strong>Atom / RSS Feed</strong>：订阅源仍以 XML 为主</li>
          <li><strong>导出与归档</strong>：PDF 表单、税务申报、电子发票等结构化文档</li>
        </ul>
      </details>

      <details>
        <summary>2. JSON 数组如何转换为 XML？</summary>
        <p>JSON 数组会展开为多个同名元素。数组项的标签名由「数组项名」选项控制（默认 <code>item</code>）。例如：</p>
        <pre><code>&#123;"tags": ["前端", "工具"]&#125;</code></pre>
        <p>转换为：</p>
        <pre><code>&lt;tags&gt;
  &lt;item&gt;前端&lt;/item&gt;
  &lt;item&gt;工具&lt;/item&gt;
&lt;/tags&gt;</code></pre>
        <p>可在选项中把「数组项名」改为更具语义的名称（如 <code>tag</code>、<code>li</code>、<code>entry</code>）。</p>
      </details>

      <details>
        <summary>3. JSON 的 key 含非法字符（空格、数字开头）怎么办？</summary>
        <p>XML 标签名有严格规则：必须以字母或下划线开头，只能包含字母、数字、连字符、下划线、点号，且不能以 <code>xml</code>（任意大小写）开头。本工具会自动修正非法标签名：</p>
        <ul>
          <li>数字开头 → 加下划线前缀（如 <code>123</code> → <code>_123</code>）</li>
          <li>含空格或特殊字符 → 替换为下划线（如 <code>"a b"</code> → <code>a_b</code>）</li>
          <li>以 xml 开头 → 加下划线前缀</li>
          <li>完全无法修正 → 退化使用数组项名作为标签</li>
        </ul>
        <p>修正会在警告区提示具体变更，便于核对。</p>
      </details>

      <details>
        <summary>4. 「扁平对象用属性」选项有什么作用？</summary>
        <p>默认情况下，对象的字段都转为子元素：</p>
        <pre><code>&lt;user&gt;
  &lt;name&gt;Aether&lt;/name&gt;
  &lt;age&gt;18&lt;/age&gt;
&lt;/user&gt;</code></pre>
        <p>开启「扁平对象用属性」后，若对象的所有字段都是基本类型（字符串 / 数字 / 布尔 / null），会转为属性形式，更紧凑：</p>
        <pre><code>&lt;user name="Aether" age="18"/&gt;</code></pre>
        <p>属性模式适合扁平的元数据对象。嵌套对象仍保持子元素结构，不会被强制转为属性。</p>
      </details>

      <details>
        <summary>5. CDATA 与字符转义有什么区别？</summary>
        <p>XML 文本节点中出现 <code>&lt;</code>、<code>&gt;</code>、<code>&amp;</code> 必须处理，否则会破坏文档结构。两种处理方式：</p>
        <ul>
          <li><strong>字符转义（默认）</strong>：替换为实体，如 <code>&lt;</code> → <code>&amp;lt;</code>。通用、兼容性最好，但长文本可读性差</li>
          <li><strong>CDATA 包裹</strong>：用 <code>&lt;![CDATA[...]]&gt;</code> 包裹，内容原样保留。适合含大量特殊字符的长文本（如 HTML 片段、代码、正则）</li>
        </ul>
        <p>开启「特殊字符用 CDATA」后，含 <code>&lt;</code>、<code>&gt;</code>、<code>&amp;</code> 的文本会自动用 CDATA 包裹。工具会处理 CDATA 中 <code>]]&gt;</code> 分隔符冲突，自动拆分为多段。</p>
      </details>

      <details>
        <summary>6. JSON 的 null 如何表示为 XML？</summary>
        <p>JSON 有 null，XML 没有原生的 null 概念。两种表示方式：</p>
        <ul>
          <li><strong>默认（空元素）</strong>：<code>&lt;note/&gt;</code> 或 <code>&lt;note&gt;&lt;/note&gt;</code>，简洁但无法区分「null」与「空字符串」</li>
          <li><strong>xsi:nil</strong>：<code>&lt;note xsi:nil="true"/&gt;</code>，遵循 XML Schema 规范，明确标记为 null，根节点自动注入 <code>xmlns:xsi</code> 命名空间</li>
        </ul>
        <p>对接 XML Schema 校验的系统建议开启 xsi:nil 模式，普通场景空元素即可。</p>
      </details>

      <details>
        <summary>7. XML 声明 &lt;?xml?&gt; 的作用是什么？</summary>
        <p>XML 声明位于文档首行，格式 <code>&lt;?xml version="1.0" encoding="UTF-8"?&gt;</code>，包含：</p>
        <ul>
          <li><strong>version</strong>：XML 版本（1.0 最常用，1.1 极少用）</li>
          <li><strong>encoding</strong>：字符编码，UTF-8 是现代默认值</li>
          <li><strong>standalone</strong>（可选）：是否依赖外部 DTD，yes / no</li>
        </ul>
        <p>XML 规范不强制要求声明，但多数解析器推荐包含。SOAP、Office 文档、Android 资源等场景要求必须有声明。可在选项中关闭。</p>
      </details>

      <details>
        <summary>8. well-formed 校验检查什么？</summary>
        <p>well-formed（良构）是 XML 文档的最低合规要求，校验包括：</p>
        <ul>
          <li>有且仅有一个根元素</li>
          <li>所有开始标签都有对应的结束标签</li>
          <li>标签嵌套正确（不允许交叉嵌套，如 <code>&lt;a&gt;&lt;b&gt;&lt;/a&gt;&lt;/b&gt;</code> 非法）</li>
          <li>属性值用引号包裹</li>
          <li>文本节点中无未转义的 <code>&lt;</code> 或 <code>&amp;</code></li>
          <li>标签名符合命名规则</li>
        </ul>
        <p>注意：well-formed 不等于 valid（有效）。valid 还需通过 DTD 或 XML Schema 校验语义约束。本工具保证生成结果 well-formed，并提供校验提示。</p>
      </details>

      <details>
        <summary>9. 与 CSV / JSON 互转、YAML 格式化工具有什么区别？</summary>
        <ul>
          <li><strong>CSV / JSON 互转</strong>（/csv-json）：CSV ↔ JSON 双向转换，面向表格数据（带表头、扁平结构）</li>
          <li><strong>YAML 格式化</strong>（/yaml）：YAML ↔ JSON 双向转换与格式化，面向配置文件</li>
          <li><strong>本工具</strong>（/json-to-xml）：JSON → XML 单向导出，面向 SOAP / SVG / Office / 旧系统对接</li>
        </ul>
        <p>三者互补：表格数据用 CSV 工具，配置文件用 YAML 工具，对接 XML 系统用本工具。需要反向转换（XML → JSON）可借助浏览器 DOMParser 自行实现。</p>
      </details>

      <details>
        <summary>10. 数据会上传到服务器吗？</summary>
        <p>不会。所有转换在浏览器本地完成，不发起任何网络请求。本工具基于纯原生 TypeScript 实现，无第三方依赖，数据不会离开你的设备。适合处理敏感配置、密钥、个人信息等不可外泄的数据。</p>
      </details>
    </section>

    <section class="related-tools" aria-labelledby="related-title">
      <h2 id="related-title">相关工具</h2>
      <ul class="related-tools__list">
        <li><a href="/json">JSON 工具</a> —— 格式化、压缩、校验 JSON 数据</li>
        <li><a href="/json-to-ts">JSON 转 TypeScript</a> —— JSON 数据转换为 TS 接口类型</li>
        <li><a href="/xml-to-json">XML 转 JSON</a> —— XML 反向转换为 JSON</li>
        <li><a href="/jsonpath">JSONPath 查询</a> —— 用 JSONPath 表达式提取 JSON 数据</li>
        <li><a href="/json-schema">JSON Schema 校验</a> —— 用 Schema 校验 JSON 结构</li>
        <li><a href="/yaml">YAML 处理</a> —— 配置文件格式互转</li>
        <li><a href="/toml">TOML 处理</a> —— 配置文件格式互转</li>
        <li><a href="/csv-json">CSV 与 JSON 转换</a> —— 表格数据与 JSON 互转</li>
        <li><a href="/html-formatter">HTML 格式化与压缩</a> —— XML 与 HTML 同源标签语言</li>
      </ul>
    </section>
  </section>
</BaseLayout>

<style is:global>
/* ============================================================
   JSON 转 XML 工具样式（jtx__ 前缀）
   ============================================================ */
.jtx__container {
  display: flex;
  flex-direction: column;
  gap: 1rem;
  margin: 1.5rem 0;
}

.jtx__panel {
  border: 1px solid var(--border, #e2e8f0);
  border-radius: 8px;
  background: var(--card-bg, #fff);
  overflow: hidden;
}

.jtx__panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0.75rem 1rem;
  border-bottom: 1px solid var(--border, #e2e8f0);
  background: var(--card-header, #f8fafc);
}

.jtx__label {
  font-weight: 600;
  font-size: 0.9rem;
  color: var(--text, #1e293b);
}

.jtx__actions {
  display: flex;
  gap: 0.5rem;
}

.jtx__btn {
  padding: 0.4rem 0.85rem;
  font-size: 0.85rem;
  border: 1px solid var(--border, #cbd5e1);
  border-radius: 6px;
  background: var(--btn-bg, #fff);
  color: var(--text, #1e293b);
  cursor: pointer;
  transition: all 0.15s ease;
}

.jtx__btn:hover:not(:disabled) {
  border-color: var(--primary, #3b82f6);
  color: var(--primary, #3b82f6);
}

.jtx__btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.jtx__btn--ghost {
  background: transparent;
}

.jtx__textarea,
.jtx__input,
.jtx__select {
  width: 100%;
  padding: 0.75rem 1rem;
  font-size: 0.9rem;
  font-family: 'Cascadia Code', 'Fira Code', 'JetBrains Mono', Consolas, monospace;
  border: none;
  border-bottom: 1px solid var(--border, #e2e8f0);
  background: var(--input-bg, #fff);
  color: var(--text, #1e293b);
  resize: vertical;
}

.jtx__textarea {
  min-height: 220px;
  line-height: 1.5;
}

.jtx__input,
.jtx__select {
  font-family: inherit;
  border: 1px solid var(--border, #cbd5e1);
  border-radius: 6px;
  padding: 0.4rem 0.6rem;
}

/* 选项面板 */
.jtx__options {
  border: 1px solid var(--border, #e2e8f0);
  border-radius: 8px;
  background: var(--card-bg, #fff);
}

.jtx__options-summary {
  padding: 0.75rem 1rem;
  cursor: pointer;
  font-weight: 600;
  font-size: 0.9rem;
  color: var(--text, #1e293b);
  user-select: none;
}

.jtx__options-summary:hover {
  color: var(--primary, #3b82f6);
}

.jtx__options-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1rem;
  padding: 0 1rem 1rem;
}

.jtx__option {
  display: flex;
  flex-direction: column;
  gap: 0.4rem;
}

.jtx__option-label {
  font-size: 0.8rem;
  color: var(--text-secondary, #64748b);
}

.jtx__option--checks {
  grid-column: 1 / -1;
  flex-direction: row;
  flex-wrap: wrap;
  gap: 1rem;
}

.jtx__check {
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
  font-size: 0.85rem;
  cursor: pointer;
  color: var(--text, #1e293b);
}

.jtx__check input[type="checkbox"] {
  width: 16px;
  height: 16px;
  cursor: pointer;
}

/* 输出区 */
.jtx__output {
  margin: 0;
  padding: 1rem;
  font-family: 'Cascadia Code', 'Fira Code', 'JetBrains Mono', Consolas, monospace;
  font-size: 0.85rem;
  line-height: 1.6;
  white-space: pre;
  overflow-x: auto;
  background: var(--code-bg, #f8fafc);
  color: var(--text, #1e293b);
  max-height: 480px;
  overflow-y: auto;
}

.jtx__error {
  margin: 1rem;
  padding: 0.75rem 1rem;
  background: #fef2f2;
  color: #b91c1c;
  border-left: 3px solid #dc2626;
  border-radius: 4px;
  font-size: 0.85rem;
}

.jtx__validation {
  margin: 0 1rem 0.75rem;
  padding: 0.6rem 0.85rem;
  border-radius: 4px;
  font-size: 0.85rem;
}

.jtx__validation--ok {
  background: #f0fdf4;
  color: #15803d;
  border-left: 3px solid #16a34a;
}

.jtx__validation--err {
  background: #fef2f2;
  color: #b91c1c;
  border-left: 3px solid #dc2626;
}

.jtx__validation ul {
  margin: 0.3rem 0 0;
  padding-left: 1.2rem;
}

.jtx__warnings {
  margin: 0 1rem 0.75rem;
  padding: 0.6rem 0.85rem;
  background: #fffbeb;
  color: #b45309;
  border-left: 3px solid #f59e0b;
  border-radius: 4px;
  font-size: 0.85rem;
}

.jtx__warnings ul {
  margin: 0.3rem 0 0;
  padding-left: 1.2rem;
}

.jtx__stats {
  margin: 0 1rem 1rem;
  padding: 0.6rem 0.85rem;
  background: var(--code-bg, #f1f5f9);
  border-radius: 4px;
  font-size: 0.8rem;
  color: var(--text-secondary, #64748b);
}

.jtx__empty {
  padding: 2rem 1rem;
  text-align: center;
  color: var(--text-secondary, #94a3b8);
  font-size: 0.9rem;
}

.jtx__notice {
  padding: 0.6rem 1rem;
  background: var(--primary-bg, #eff6ff);
  color: var(--primary, #3b82f6);
  border-radius: 6px;
  font-size: 0.85rem;
  text-align: center;
}

.jtx__links {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem 1rem;
  align-items: center;
  padding: 0.75rem 1rem;
  font-size: 0.85rem;
  color: var(--text-secondary, #64748b);
  border-top: 1px solid var(--border, #e2e8f0);
}

.jtx__links a {
  color: var(--primary, #3b82f6);
  text-decoration: none;
}

.jtx__links a:hover {
  text-decoration: underline;
}

/* 暗色模式 */
:global([data-theme="dark"]) .jtx__panel,
:global([data-theme="dark"]) .jtx__options {
  background: var(--card-bg, #1e293b);
  border-color: var(--border, #334155);
}

:global([data-theme="dark"]) .jtx__panel-header {
  background: var(--card-header, #0f172a);
  border-bottom-color: var(--border, #334155);
}

:global([data-theme="dark"]) .jtx__textarea,
:global([data-theme="dark"]) .jtx__input,
:global([data-theme="dark"]) .jtx__select {
  background: var(--input-bg, #0f172a);
  color: var(--text, #e2e8f0);
  border-bottom-color: var(--border, #334155);
}

:global([data-theme="dark"]) .jtx__input,
:global([data-theme="dark"]) .jtx__select {
  border-color: var(--border, #334155);
}

:global([data-theme="dark"]) .jtx__btn {
  background: var(--btn-bg, #1e293b);
  color: var(--text, #e2e8f0);
  border-color: var(--border, #334155);
}

:global([data-theme="dark"]) .jtx__output {
  background: var(--code-bg, #0f172a);
  color: var(--text, #e2e8f0);
}

:global([data-theme="dark"]) .jtx__stats {
  background: var(--code-bg, #0f172a);
  color: var(--text-secondary, #94a3b8);
}

:global([data-theme="dark"]) .jtx__notice {
  background: rgba(59, 130, 246, 0.15);
}

:global([data-theme="dark"]) .jtx__links {
  border-top-color: var(--border, #334155);
}

/* 移动端响应式 */
@media (max-width: 768px) {
  .jtx__options-grid {
    grid-template-columns: 1fr;
  }

  .jtx__option--checks {
    gap: 0.6rem;
  }

  .jtx__textarea {
    min-height: 160px;
    font-size: 0.8rem;
  }

  .jtx__output {
    font-size: 0.75rem;
    max-height: 360px;
  }

  .jtx__actions {
    gap: 0.4rem;
  }

  .jtx__btn {
    padding: 0.35rem 0.65rem;
    font-size: 0.8rem;
  }
}
</style>
