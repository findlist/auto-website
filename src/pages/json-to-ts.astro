---
import BaseLayout, { type SiteMeta } from '../layouts/BaseLayout.astro';
import JsonToTsTool from '../components/JsonToTsTool.tsx';

const meta: SiteMeta = {
  title: 'JSON 转 TypeScript 接口生成器 - 在线推断类型并导出 interface',
  description:
    '在线 JSON 转 TypeScript 工具：自动推断类型生成 interface 声明，支持联合类型合并、可选字段检测、嵌套提取与结构去重。全本地处理，零广告零追踪。',
  jsonLd: {
    '@context': 'https://schema.org',
    '@type': 'WebApplication',
    name: 'JSON 转 TypeScript 接口生成器',
    description: '在线 JSON 转 TypeScript 工具，自动推断类型生成 interface 声明，支持联合类型、可选字段、嵌套类型提取与去重，全本地处理。',
    applicationCategory: 'DeveloperApplication',
    operatingSystem: 'Any (Web Browser)',
    inLanguage: 'zh-CN',
    offers: {
      '@type': 'Offer',
      price: '0',
      priceCurrency: 'CNY',
    },
  },
};
---
<BaseLayout meta={meta}>
  <section class="container">
    <div class="page-hero page-hero--tool">
      <h1>JSON 转 TypeScript 接口生成器</h1>
      <p>
        粘贴 JSON 数据自动推断 TypeScript 类型，生成 export interface 声明。支持联合类型合并（数组元素类型不同或同名字段类型不同时自动合并为联合）、可选字段检测（数组中对象字段不一致时标记 ?:）、嵌套对象提取为独立 interface、相同结构去重复用。递归遍历 JSON 树，纯原生 TypeScript 零依赖实现，所有数据在浏览器本地处理。
      </p>
    </div>

    <!-- client:load 让 React 岛屿在页面加载时立即水合，保证交互即时可用 -->
    <JsonToTsTool client:load />

    <!-- 常见问题：补充 SEO 内容 -->
    <section class="json-faq" aria-labelledby="faq-title">
      <h2 id="faq-title">常见问题</h2>
      <details>
        <summary>什么是 JSON 转 TypeScript？为什么需要它？</summary>
        <p>JSON 转 TypeScript 是从 JSON 数据反向推断 TypeScript 类型定义的过程。手动为 API 响应、配置文件、Mock 数据编写 interface 既繁琐又易错，本工具自动完成。常见场景：①从后端 API 响应生成前端请求函数的类型定义；②为本地 JSON 配置文件生成类型，获得编辑器自动补全；③将 Mock 数据转为类型，用于单元测试；④阅读第三方 API 返回结构。生成结果可直接粘贴到 <code>.ts</code> 文件中使用。</p>
      </details>
      <details>
        <summary>如何处理数组中元素类型不同的情况？</summary>
        <p>本工具递归合并数组所有元素的类型。当元素类型不同时合并为联合类型：①基本类型混合（如 <code>[1, "a", true]</code>）→ <code>(number | string | boolean)[]</code>；②对象字段类型不同（如 <code>[&#123;a:1&#125;, &#123;a:"x"&#125;]</code>）→ <code>&#123; a: number | string &#125;[]</code>；③对象字段并集（如 <code>[&#123;a:1&#125;, &#123;b:2&#125;]</code>）→ <code>&#123; a?: number; b?: number &#125;[]</code>（缺失字段标记可选）。联合类型输出稳定（按签名排序），避免每次生成结果不一致。</p>
      </details>
      <details>
        <summary>如何检测可选字段？</summary>
        <p>当数组中多个对象的字段不一致时，缺失的字段会被标记为可选（?:）。例如 <code>[&#123;id:1, name:"A"&#125;, &#123;id:2&#125;]</code> 中 <code>name</code> 在第二个对象缺失，生成 <code>&#123; id: number; name?: string &#125;</code>。判定规则：字段在数组所有对象中都出现 → 必选；任一对象缺失 → 可选。可在选项面板关闭「可选字段」开关，强制所有字段必选（适合字段语义上必填但部分样本缺失的场景）。注意：单个对象（非数组元素）的字段始终必选，因为 JSON 对象本身定义了完整结构。</p>
      </details>
      <details>
        <summary>嵌套对象如何提取为独立 interface？</summary>
        <p>为避免生成过深的嵌套类型，本工具将对象值字段提取为独立 interface 声明。例如 <code>&#123;user:&#123;name:string&#125;&#125;</code> 生成两个 interface：<code>interface Root &#123; user: User &#125;</code> 与 <code>interface User &#123; name: string &#125;</code>。命名规则：字段名首字母大写拼接父级名（如 <code>Root.user</code> → <code>RootUser</code>，数组元素用 <code>RootItem</code>）。相同结构的对象会复用同一个 interface 名（去重），避免重复声明。</p>
      </details>
      <details>
        <summary>interface 去重是如何工作的？</summary>
        <p>本工具基于「结构签名」对 interface 去重。签名由字段名 + 字段类型签名按字典序拼接而成，相同结构的对象视为同一类型。例如 <code>&#123;a:1, b:2&#125;</code> 与 <code>&#123;b:3, a:"x"&#125;</code> 的字段名集合相同（a 和 b），但 a 的类型不同（number vs string），签名不同，会生成两个 interface。而 <code>&#123;a:1&#125;</code> 与 <code>&#123;a:2&#125;</code> 签名相同，复用同一 interface。去重保证输出简洁，避免冗余声明。</p>
      </details>
      <details>
        <summary>如何处理 null 值？</summary>
        <p>JSON 中的 <code>null</code> 在 TypeScript 中对应 <code>null</code> 类型。本工具默认保留 null 为联合类型的一部分：①字段值为 null（如 <code>"v": null</code>）→ <code>v: null</code>；②数组含 null 与其他类型（如 <code>[1, null]</code>）→ <code>(number | null)[]</code>；③字段在不同对象中分别为值与 null（如 <code>[&#123;a:1&#125;, &#123;a:null&#125;]</code>）→ <code>a: number | null</code>。可选字段开关仅控制 <code>?:</code> 标记，不影响 null 合并。如需将 null 视为 undefined，可在生成后手动替换。</p>
      </details>
      <details>
        <summary>支持哪些 JSON 类型？</summary>
        <p>支持 JSON 规范的全部类型：①字符串 → <code>string</code>；②数字（整数、浮点、科学计数法）→ <code>number</code>；③布尔 → <code>boolean</code>；④null → <code>null</code>；⑤数组 → <code>T[]</code> 或 <code>Array&lt;T&gt;</code>；⑥对象 → <code>interface</code>。空数组元素类型推断为 <code>any</code>。空对象生成 <code>interface Name &#123;&#125;</code>。顶层为基本类型时输出 <code>type Root = string</code> 别名而非 interface。</p>
      </details>
      <details>
        <summary>字段名不合法时会怎样？</summary>
        <p>TypeScript 字段名需为合法标识符（字母/下划线/$ 开头，后接字母数字下划线/$），否则需用引号包裹。本工具自动检测：①合法名（如 <code>name</code>、<code>_id</code>、<code>$ref</code>）原样输出；②非法名（如 <code>"my-key"</code>、<code>"2nd"</code>、<code>"a b"</code>）用 JSON.stringify 包裹为字符串字面量（如 <code>"my-key": string</code>）。中文键名（如 <code>"名称"</code>）会被引号包裹，TS 允许但部分 linter 会警告。</p>
      </details>
      <details>
        <summary>与 JSON Schema 工具有何区别？</summary>
        <p>两者定位不同：①本工具生成 TypeScript <code>interface</code> 声明，用于编辑器类型检查与自动补全，是开发期类型定义；②<a href="/json-schema">JSON Schema 工具</a>生成 JSON Schema（draft-07），用于运行时数据校验，是数据约束规范。例如 <code>"age": 18</code>，本工具输出 <code>age: number</code>，JSON Schema 输出 <code>&#123;"type":"number"&#125;</code>。如需运行时校验，请用 JSON Schema；如需编辑器类型提示，请用本工具。两者可结合使用：先用本工具生成类型定义，再用 JSON Schema 做运行时校验。</p>
      </details>
      <details>
        <summary>与 JSON / JSONPath / 格式化工具有何联动？</summary>
        <p>本工具是 JSON 工具矩阵的一员：①<a href="/json">JSON 工具</a>做格式化、压缩、校验、树形浏览；②<a href="/jsonpath">JSONPath 工具</a>做数据查询与提取；③<a href="/json-schema">JSON Schema 工具</a>做运行时校验；④本工具做类型推断。典型流程：用 JSON 工具格式化压缩后的 API 响应 → 用 JSONPath 提取特定字段 → 用本工具生成 TypeScript 类型 → 用 JSON Schema 生成校验规则。所有工具共享 JSON 数据处理能力，互为补充。</p>
      </details>
      <details>
        <summary>数据会上传到服务器吗？</summary>
        <p>不会。本工具完全在浏览器内运行，所有类型推断、interface 生成、去重、序列化逻辑由 JavaScript 在你本地执行，输入内容不会发送到任何服务器。</p>
      </details>
    </section>

    <section class="related-tools" aria-labelledby="related-title">
      <h2 id="related-title">相关工具</h2>
      <ul class="related-tools__list">
        <li><a href="/json">JSON 工具</a> —— 格式化、压缩、校验 JSON 数据</li>
        <li><a href="/json-to-xml">JSON 转 XML</a> —— JSON 与 XML 标签结构互转</li>
        <li><a href="/xml-to-json">XML 转 JSON</a> —— XML 反向转换为 JSON</li>
        <li><a href="/jsonpath">JSONPath 查询</a> —— 用 JSONPath 表达式提取 JSON 数据</li>
        <li><a href="/json-schema">JSON Schema 校验</a> —— 用 Schema 校验 JSON 结构</li>
        <li><a href="/yaml">YAML 处理</a> —— 配置文件格式互转</li>
        <li><a href="/toml">TOML 处理</a> —— 配置文件格式互转</li>
        <li><a href="/csv-json">CSV 与 JSON 转换</a> —— 表格数据与 JSON 互转</li>
        <li><a href="/regex">正则表达式</a> —— TS 类型生成时配合正则提取字段</li>
      </ul>
    </section>

    <!-- 相关博客：工具页反向内链，引导用户深度阅读（脚本批量生成，请勿手动修改） -->
    <section class="related-blogs" aria-labelledby="related-blogs-title">
      <h2 id="related-blogs-title">相关博客</h2>
      <ul class="related-blogs__list">
        <li>
          <a href="/blog/json-to-typescript-interface-guide">JSON 转 TypeScript 接口原理：类型推断、联合合并与 interface 去重</a><span class="related-blogs__desc">深入解析 JSON 转 TypeScript 接口原理：递归类型推断算法、数组元素类型合并、联合类型去重排序、可选字段检测、嵌套对象提取为独立 interface，附原生实现…</span>
        </li>
      </ul>
    </section>
  </section>
</BaseLayout>

<style is:global>
  /* JSON 转 TS 工具专属样式：复用 .jsontool/.jsfmt 基础样式，仅补充差异部分 */
  .jts__tag {
    display: inline-block;
    padding: 2px 10px;
    font-size: 12px;
    font-weight: 600;
    color: var(--color-accent, #0969da);
    background: var(--color-bg-soft, #f6f8fa);
    border: 1px solid var(--color-border, #d0d7de);
    border-radius: 4px;
    letter-spacing: 0.3px;
  }

  .jts__options {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    align-items: center;
    padding: 8px 12px;
    margin-bottom: 12px;
    background: var(--color-bg-soft, #f6f8fa);
    border-radius: 6px;
  }

  .jts__name-input {
    width: 100px;
    padding: 2px 6px;
    font-size: 13px;
    font-family: inherit;
    border: 1px solid var(--color-border, #d0d7de);
    border-radius: 4px;
    background: var(--color-bg, #fff);
    color: var(--color-text, #1f2328);
  }

  .jts__name-input:focus {
    outline: 2px solid var(--color-accent, #0969da);
    outline-offset: -1px;
  }

  /* 移动端响应式 */
  @media (max-width: 640px) {
    .jts__options {
      gap: 8px;
      padding: 6px 8px;
    }

    .jts__name-input {
      width: 80px;
      font-size: 12px;
    }
  }
</style>
