# UI 优化记录 2026-08-01 (website)

## 修改文件

### src/components/CsvJsonTool.tsx — 错误提示 a11y 一致性

**问题**：L783 错误提示 `<div className="jsontool__error">` 缺少 `role="alert"`，屏幕阅读器不会主动播报。父容器 L781 已有 `role="status" aria-live="polite"`，但错误应立即播报（assertive）。同文件 L758 `csvtool__warnings` 已有 `role="alert"`，全站 15+ 工具错误容器均已添加 `role="alert"`（昨日 AesTool/FindReplaceTool/DnsTool/Base64Tool/Base32Tool 已修复），此为遗漏项。

**修改**：
- L783：`<div className="jsontool__error">` → `<div className="jsontool__error" role="alert">`

### src/components/HexTool.tsx — 错误提示 a11y 一致性

**问题**：L382 错误提示 `<div className="jsontool__error">` 缺少 `role="alert"`，与 CsvJsonTool 同类问题。父容器 L380 已有 `role="status" aria-live="polite"`，但错误应立即播报。

**修改**：
- L382：`<div className="jsontool__error">` → `<div className="jsontool__error" role="alert">`

**评分**：CsvJsonTool Score=35, HexTool Score=35, C=5, R=1（同一关联问题：错误提示缺 role=alert）

## 验证

- `npm run check`：本地 Node v14.21.2 不满足 Astro >=18.20.8 要求，无法运行。修改仅为标准 HTML 属性添加，风险可忽略；服务器端 Node 18+ 构建不受影响（与 2026-07-31 同一环境限制）
- `npm run build`：同上，本地无法运行；服务器构建不受影响

## 下次方向

- CssFormatterTool.tsx、HashTool.tsx(L446)、HtmlEntityTool.tsx、HtmlFormatterTool.tsx、Ieee754Tool.tsx 的错误容器仍缺 `role="alert"`，可后续处理
