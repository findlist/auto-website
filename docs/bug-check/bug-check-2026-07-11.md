# Bug 检查与修复报告 - auto-website

## 检查时间
2026-07-11 00:50

## 检查范围

### 基线检查
- `npm run check`（astro check）：通过（0 errors, 0 warnings, 1 hint）
  - hint 为 `clipboard.ts` 中 `document.execCommand('copy')` 已废弃提示（兼容性回退，非 bug）
- `npm run build`：通过（258 页面，31.80s 构建完成，无错误）

### 代码审查范围
- **src/components/**：46 个工具组件，重点审查 22 个逻辑复杂组件（加密/JWT/JSON/Regex/QR 等类别）
- **src/pages/**：50+ 页面（首页、47 工具页、博客列表/详情/标签页、RSS、about、privacy）
- **src/utils/**：26 个工具函数（aes、jwe、jwtSign、jwtVerify、jsonPath、jsonSchema、punycode 等加密/解析逻辑）
- **src/layouts/BaseLayout.astro**：布局与 SEO 元数据
- **src/styles/global.css**：全局样式
- **astro.config.mjs / tsconfig.json / content.config.ts**：项目配置
- **public/robots.txt / og-image.svg**：SEO 资源
- **git diff HEAD~1**：最近变更（json.astro、jwt.astro、global.css 样式优化，BUG-35/36 域名清理）

### 安全检查
- `eval / new Function`：未使用（代码中仅在博客文章中作为反面教材提及）
- `innerHTML / dangerouslySetInnerHTML`：2 处使用（MarkdownTool、SqlTool），均已做 XSS 防护
  - MarkdownTool：escapeHtml + sanitizeUrl（协议白名单拦截 javascript:/data:）
  - SqlTool：highlightSql 内部 escapeHtml 转义后再拼接 span 标签
- `set:html`：1 处（BaseLayout JSON-LD），使用 JSON.stringify 输出，安全
- CSP 违规：无内联事件处理器，无外部脚本注入

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|------|----------|------|----------|----------|----------|
| 1 | P1 | src/utils/jwe.ts、src/components/JweTool.tsx | PBES2 算法的 PBKDF2 salt 构造不符合 RFC 7518 4.8.1.1。解密端（jwe.ts:681）直接用 p2s 解码的原始 salt_input 作为 PBKDF2 salt，加密端（JweTool.tsx:150）同理。缺少 RFC 规定的 `alg(UTF-8) \|\| salt_input` 前缀拼接，导致本工具生成的 PBES2 JWE 无法被标准库（jose 等）解密，反之标准库生成的 PBES2 JWE 本工具也无法解密。两端自加密自解密因错误一致而互通，但与外部标准库不兼容。 | 两端同步修复：在 PBKDF2 deriveBits/deriveKey 调用前，构造完整 salt = `encodeUtf8(alg) + salt_input`。p2s header 仍存储原始 salt_input（符合 RFC），完整 salt 在运行时拼接。 | 6868941 |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|------|----------|------|----------|------------|
| 1 | P1 | src/utils/jsonPath.ts:750-757 | 过滤表达式 `[?(...)]` 中 `$` 根路径引用错误。`evaluateOperand` 第 754 行 `evaluate(node, operand.segments)` 中 `node` 是当前候选元素而非原始根节点，导致 `$.threshold` 这类在过滤中引用根节点字段的查询返回错误结果（静默返回空）。例如 `$.items[?(@ > $.threshold)]` 对 `{threshold:10, items:[1,5,15,20]}` 应返回 `[15,20]`，实际返回 `[]`。 | 修复需修改 6 个函数签名（evaluate → evaluateSegment → evaluateFilter → evaluateFilterNode → evaluateCompare → evaluateOperand）传递 rootData 参数，属于大范围改动，可能引入新问题。记录为需评估，建议后续专项修复。 |
| 2 | P1 | public/og-image.svg、src/layouts/BaseLayout.astro:33,84,91 | 全站 OG 图片为 SVG 格式，主流社交平台（Twitter/Facebook/LinkedIn/WeChat/Telegram）不渲染 SVG og:image，导致 258 页面社交分享预览图缺失。BaseLayout 同时缺少 og:image:width/height/type meta 标签。 | 核心修复需生成 1200x630 PNG/JPG 图片替换 SVG，属于设计资源生成，超出代码修复范围。补充 og:image meta 标签在图片仍为 SVG 时无实际效果。建议后续提供 PNG 资源后一并补充 meta 标签。 |

## 记录的轻微问题（P2，未修复）

- **clipboard.ts:39** `document.execCommand('copy')` 已废弃（check hint），已有 navigator.clipboard 回退逻辑，兼容性处理得当
- **8/47 工具页 hero 样式不一致**：json/jwt/color/diff/hash/password/qr/regex 使用 `page-hero--tool` 变体，其余 39 个用默认 `page-hero`。从 git diff 可知正在渐进式迁移，非 bug
- **QrTool/PasswordTool/DiffTool/CronTool** 的 `noticeTimer` ref 在组件卸载时未 clearTimeout，但 setTimeout 1.5s 后自动释放，不构成内存泄漏
- **博客列表无分页**：41 篇全量渲染，当前数量可接受
- **首页 /json/ 带尾斜杠**而其余工具链接不带，一致性小问题不影响功能
- **tsconfig.json** `exclude: ["dist"]` 覆盖了 strict 默认的 node_modules 排除（TypeScript 隐式排除仍生效）

## 验证结果
- 修复后 check：通过（0 errors, 0 warnings, 1 hint）
- 修复后 build：通过（258 页面，12.97s 构建完成，无错误）
- 修复已提交并推送：commit 6868941 → origin/main
