# Bug 检查与修复报告 - auto-website

## 检查时间
2026-07-26 00:25

## 检查范围

### 自动化检查
- `npm run check`（astro check）：基线通过 — 0 errors / 0 warnings / 4 hints（hints 均为历史已知项：`scripts/seo-audit.mjs` 未使用的 import、`src/utils/clipboard.ts` 中 `document.execCommand` 已废弃提示，非本次变更引入）。
- `npm run build`：基线通过 — 1026 page(s) built in 29.09s，`[build] Complete!`。

### 最近一次提交变更分析（git diff HEAD~1）
HEAD~1 提交 `956a281 docs: 第 139 轮进度沉淀（时间处理工具链长尾 SEO 博客）`，变更内容：
- 仅修改 `memory/20260725/topics.md`（+55 行进度沉淀记录），无任何代码改动。

### 最近两次提交变更分析（git diff HEAD~2 HEAD）
HEAD 提交 `bd626ed feat: 新增时间处理工具链长尾 SEO 博客，5 工具协同端到端工作流`，变更内容：
- 新增 `src/content/blog/time-processing-toolchain-guide.md`（+336 行）
- 5 个工具页（`cron.astro`、`lorem.astro`、`time-unit.astro`、`timestamp.astro`、`timezone.astro`）各 +3 行，仅在「相关博客」`<ul>` 中追加一个 `<li>` 链接
- 审查结论：纯内容追加，链接指向的博客文件均存在，无逻辑改动、无类型问题、无客户端/服务端边界问题、无 SEO 缺失。

### 代码审查范围
- 加密 / 解密 / 验签 utils：`src/utils/aes.ts`、`src/utils/jwe.ts`、`src/utils/jwtSign.ts`、`src/utils/jwtVerify.ts` — 实现完善，含 PBKDF2 迭代次数下限 DoS 防护（≥10000）、alg 白名单防 `alg=none` 攻击、`RSA1_5` 已从默认支持列表移除并拦截、EC 系列 JWK 的 `kty`/`crv` 字段校验、Concat KDF 完整实现。未发现逻辑错误。
- 全局 `eval` / `new Function` / `dangerouslySetInnerHTML` / `innerHTML` 扫描：8 个组件命中（`IfFunctionTool`、`PositionAreaTool`、`SubgridTool`、`ViewTransitionTool`、`LightDarkTool`、`MarkdownTool`、`SqlTool`、`HtmlEntityTool`），逐一审查后确认：
  - `MarkdownTool`（自实现 `escapeHtml` + `sanitizeUrl`，所有用户输入均经过转义）
  - `SqlTool`（`highlightSql` 中先 `escapeHtml` 再拼接 `<span>` 高亮标签）
  - `IfFunctionTool`（`bc1a33f` 提交已修复两处真实 XSS，`prop` 与 `hitPrediction.reason` 均已 `escapeHtml`）
  - `ViewTransitionTool`（`previewA`/`previewB` 来自硬编码 `PRESETS` 常量，非用户输入；iframe 使用 `sandbox="allow-same-origin allow-scripts"`，并通过 `JSON.stringify` 注入）
  - `PositionAreaTool`/`SubgridTool`/`LightDarkTool`（插入变量均来自预设常量、枚举映射或 `<select>` 受限值，非用户自由输入）
  - `HtmlEntityTool`（`document.cookie` 仅出现在 XSS 演示字面量中，无真实调用）
  - 均安全。
- `src/utils/jsonPath.ts`：`=~` 正则匹配运算已做 ReDoS 防护（右操作数长度 ≤ 1000、被匹配字符串长度 ≤ 100000、`try/catch` 包裹非法正则）。
- `src/components/RegexTool.tsx`：含输入长度上限 10000、匹配数上限 5000、零宽匹配保护，`new RegExp` 在 `try/catch` 中调用。
- 时间处理工具链 5 个组件（`CronTool`、`LoremTool`、`TimeUnitTool`、`TimestampTool`、`TimeZoneTool`）以及对应 utils（`cron.ts`、`lorem.ts`、`timeUnit.ts`、`timezone.ts`）：均无 `dangerouslySetInnerHTML`/`innerHTML`/`eval`，实现完善。
- 编码工具组件（`Base64Tool`、`HexTool`、`JsonTool`、`HashTool`）：UTF-8 处理、URL 安全变体、Base64 padding 补齐、Hex dump 格式、SubtleCrypto 调用均正确，无逻辑错误。
- 布局 `src/layouts/BaseLayout.astro`：SEO 元数据完整（canonical / OG / Twitter Card / JSON-LD WebSite + WebApplication / RSS / robots），分页 prev/next 信号规范化，占位域名兜底覆盖（`PLACEHOLDER_ORIGIN` 检测），未发现问题。
- 配置 `astro.config.mjs`：`rehypeLazyImages` 幂等递归补齐 `loading="lazy"` 与 `decoding="async"`，`output: 'static'` + `compressHTML` + `inlineStylesheets: 'auto'`，未发现问题。
- `src/utils/site.ts`：`getSiteUrl` 采用 fail-fast 策略（未配置 `site` 时构建直接报错），`normalizeUrlTrailingSlash` 处理规则正确（根路径/含扩展名 URL 保持原样，目录形式路径追加尾部斜杠）。
- `public/robots.txt`：欢迎搜索引擎抓取，sitemap 链接正确指向 `https://website.niuzi.asia/sitemap-index.xml`。
- 客户端 / 服务端边界：100 个 `.astro` 页面均正确使用 `client:load` / `client:visible` / `client:idle` 指令，未发现 island 组件使用错误。

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|
| — | — | — | 本次检查未发现 P0/P1 问题。 | — | — |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| — | — | — | — | 本次检查未发现需要评估的 P0/P1 问题。 |

## 记录的轻微问题（P2，未修复）

- `scripts/seo-audit.mjs` 第 3-4 行：`existsSync`、`normalize`、`sep` 三个 import 未使用（ts(6133)）。属历史已知 hints，非本次变更引入，且为独立脚本不影响运行，按约束 P2 不修复。
- `src/utils/clipboard.ts` 第 39 行：`document.execCommand('copy')` 已废弃（ts(6387)）。已有 `clipboard.ts` 中通过 `navigator.clipboard.writeText` 优先路径并回退到 `execCommand`，废弃提示无法消除，属历史已知 hints，P2 不修复。
- `src/components/LightDarkTool.tsx` 第 466 行：`<code>${config.colorScheme}</code>` 中 `config.colorScheme` 来自 `<select>` 下拉框（受限值，无法直接输入任意字符），理论 XSS 风险极低。同文件未引入 `escapeHtml`，为保持最小修改未处理。建议后续统一引入转义工具。
- `src/components/SubgridTool.tsx` `buildExplain`（第 386/390 行）：`tracksToCss(parent.columns)` 输出由 `<select>`（fr/px/%/auto）与 `<input type="number">` 组成，受限输入无法构造恶意 HTML。同文件未引入 `escapeHtml`，按最小修改原则未处理。
- `src/components/PositionAreaTool.tsx` `buildExplain`：插入变量均来自预设常量或枚举映射（`KEYWORD_MAP`、`SYSTEM_LABELS`），非用户自由输入，无 XSS 风险。
- `src/components/ViewTransitionTool.tsx` `buildPreviewHtml`：`previewA`/`previewB` 来自硬编码 `PRESETS` 常量，非用户输入；iframe 使用 `sandbox="allow-same-origin allow-scripts"`，并通过 `JSON.stringify` 注入到 `<script>`，无 XSS 风险。

## 验证结果
- 修复后 `npm run check`：通过（0 errors / 0 warnings / 4 hints，与基线一致）
- 修复后 `npm run build`：通过（1026 page(s) built，`[build] Complete!`）
- 提交：本次未发现需修复的 P0/P1 问题，按约束未创建空提交。
