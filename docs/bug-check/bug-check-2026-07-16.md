# Bug 检查与修复报告 - auto-website

## 检查时间
2026-07-16 00:40

## 检查范围

### 自动化检查
- `npm run check`（astro check）：**通过** — 0 errors / 0 warnings / 1 hint（`clipboard.ts:39` `document.execCommand` 已废弃提示，属浏览器 API 历史遗留，不影响功能）
- `npm run build`（astro build）：**通过** — 665 page(s) built in 21.87s，`[build] Complete!`，sitemap 正常生成
- 项目无 lint / test 脚本（Astro 项目）

### 代码审查范围
- 最近一次提交 `540301c`（docs: 沉淀第 52 轮进度，新增 CSS interpolate-size 尺寸插值动画生成器）涉及变更：README.md、src/pages/about.astro、src/pages/blog/[...slug].astro、src/pages/index.astro、src/styles/global.css、memory/20260715/topics.md，均为文档沉淀与样式微调，无逻辑问题
- 工作区并行流程未提交变更：about.astro（page-hero 结构包装）、blog/[...slug].astro（h2/h3/img/hr 装饰样式）、global.css（prose 排版体系完善、page-hero--tool 大屏响应式），均为纯 CSS/HTML 结构调整，无 P0/P1 问题
- 新增组件审查（2026-07-15 第 52 轮新增的 9 个 CSS 2024 特性工具 + 1 个已有工具）：
  - `InterpolateSizeTool.tsx`（797 行）— CSS interpolate-size 尺寸插值动画生成器，8 组预设，calc-size() 支持，折叠/展开预览
  - `StartingStyleTool.tsx`（672 行）— CSS @starting-style 入场动画生成器，三种场景，嵌套/独立语法双模式，PreviewArea 通过 useEffect 注入 style 标签（@starting-style 必须用 CSS 规则声明）
  - `SubgridTool.tsx`（769 行）— CSS subgrid 子网格生成器，父子双层可视化预览
  - `ViewTransitionTool.tsx`（825 行）— CSS view-transition 视图过渡生成器，同文档/跨文档双模式
  - `ContainTool.tsx`（602 行）— CSS contain + content-visibility 性能优化生成器，IntersectionObserver 演示屏幕外跳过渲染
  - `LightDarkTool.tsx`（702 行）— CSS light-dark() 暗色模式颜色函数生成器，WCAG 对比度参考
  - `ScrollTimelineTool.tsx`（870 行）— CSS scroll-driven 动画生成器，三种时间线
  - `TextWrapTool.tsx`（601 行）— CSS text-wrap 文本换行排版优化生成器，5 种值
  - `ScopeTool.tsx`（585 行）— CSS @scope 作用域生成器，多 @scope 块管理，甜甜圈作用域
  - `ExifTool.tsx`（504 行）— EXIF 元数据查看器，exifr 本地解析，50MB 文件上限
- 安全维度：eval / new Function / dangerouslySetInnerHTML / innerHTML / document.write / CSP / setTimeout(stringArg) / setInterval(stringArg)
- 客户端/服务端边界：100 个 .astro 页面 `client:load` 指令一致性
- SEO：BaseLayout meta 标签、blog 结构化数据、sitemap、robots.txt
- 配置：astro.config.mjs（site='https://website.niuzi.asia'，output='static'，compressHTML=true，inlineStylesheets='auto'）

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|

> 本轮审查未发现 P0/P1 问题，未进行代码修复，未创建提交（遵循约束："如果没有发现需要修复的 P0/P1 问题，不创建空提交"）。

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| 1 | P1→P2 | src/layouts/BaseLayout.astro | 缺少 CSP（Content-Security-Policy）meta 标签，站点无第二道 XSS 防线 | 添加 CSP 可能影响 Astro 岛屿水合（内联脚本）与 React 内联样式，需 `'unsafe-inline'` 才不破坏功能；且 meta 标签不支持 `frame-ancestors` 等指令。属于"缺失安全加固"而非"活跃安全漏洞"（已审查所有 dangerouslySetInnerHTML 均做了转义），需评估上线后用 Report-Only 模式观察再切换强制模式（沿用 2026-07-15 评估结论） |
| 2 | P2 | src/components/ContainerTool.tsx | 维度 ButtonGroup 的 `disabled={... ? false : false}` 死代码三元；height 维度查询预览命中判定与生成 CSS 语义不一致；setQueries updater 内调用 setSelectedId 反模式 | 均为组件预览限制或反模式，功能正确；修复需 ButtonGroup 支持 per-option disabled / 引入预览高度变量 / 拆分 updater，超出"最小必要代码修复"范围（沿用 2026-07-15 评估结论） |
| 3 | P2 | src/components/AnimationTool.tsx | useEffect 中 `setPreviewKey((k) => k + 1)` 触发额外渲染；异步操作无 AbortController 保护 | 性能反模式与防御性增强，功能正确；修复属行为重构（沿用 2026-07-15 评估结论） |
| 4 | P2 | src/components/RegexTool.tsx | `runReplace` 替换字符串处理顺序为 `$<name>` → `$1` → `$$`，命名组值包含 `$1` 字符串会被二次替换 | 边界场景，需重构为单次扫描 token 解析；当前顺序对常见用例无影响（沿用 2026-07-15 评估结论） |
| 5 | P2 | src/components/JwtSignTool.tsx | `handleGenRsaKey` / `handleGenEcKey` / `handleSign` 异步操作无 AbortController 或 mountedRef 保护 | React 18 不再 warn 且不影响功能；修复需引入 ref 标志，属防御性增强（沿用 2026-07-15 评估结论） |
| 6 | P2 | src/components/RegexBenchmarkTool.tsx | `setTimeout` 未在卸载时 clearTimeout | 防御性清理，功能基本正常；修复需 useRef 保存 timer id + cleanup（沿用 2026-07-15 评估结论） |
| 7 | P2 | src/pages/index.astro:11 | 首页 meta description 约 1000+ 字符，列举 60+ 工具名，远超 Google 推荐的 160 字符，存在关键词堆砌风险 | SEO 优化非 bug；精简需重新撰写文案，属内容调整（沿用 2026-07-15 评估结论） |

## 记录的轻微问题（P2/P3，未修复）

- **[P3]** `src/components/LayerTool.tsx:484` — `moveLayer` 内 `const target = idx + dir` 与外部 state `target`（级联分析选择器）同名遮蔽，当前作用域不冲突但易致维护误解
- **[P3]** `src/components/ScrollSnapTool.tsx:311` — `display: isBoth ? 'flex' : 'flex'` 三元两侧相同，死代码；若 `both` 应为 `grid` 则是 bug，需确认意图
- **[P3]** `src/components/AnimationTool.tsx:266` — `updateKeyframe` 的 `field: keyof KeyframeState` 包含 `'id'`，误传会覆盖 string 类型 id 为 number；建议改为 `Exclude<keyof KeyframeState, 'id'>`
- **[P3]** `src/components/SqlTool.tsx:176` — `Number(e.target.value) as 2 | 4` 不安全类型断言，当前 select 只有 2/4 选项，运行时安全
- **[P3]** `src/components/JsonSchemaTool.tsx:208` — 空格键未 `preventDefault`，按空格触发错误路径点击时会滚动页面
- **[P3]** `src/components/HashTool.tsx:163-167` — `useMemo` 中 `new TextEncoder()` 在 SSR 阶段执行，Node 18+ 全局可用但旧版本有风险
- **[P3]** `src/components/JwtTool.tsx:139,145` / `JwtVerifyTool.tsx:66` — `atob` 在 SSR 中依赖 Node 18+ 全局，当前初始 input 为空提前返回不触发，但未来载入示例 token 可能崩溃
- **[P3]** 多个组件 — `setTimeout(() => setCopied(false), 1500)` 散布在几乎所有工具组件中，卸载后仍 setState；建议统一通过 `useFlashState` 自定义 hook 管理

## 验证结果
- 修复后 `npm run check`：**通过**（0 errors / 0 warnings / 1 hint，`clipboard.ts:39` `document.execCommand` 已废弃提示）
- 修复后 `npm run build`：**通过**（665 page(s) built in 21.87s，`[build] Complete!`，sitemap 正常生成）
- 本轮未进行代码修复，上述为当前工作区基线验证结果（含并行流程未提交的样式优化变更）

## 安全审查结论

项目整体安全基线良好：
- **无 eval / new Function / setTimeout(stringArg) / setInterval(stringArg)** 调用（仅 blog 文档中作为说明文字）
- **无 document.write / insertAdjacentHTML** 调用
- **4 处 dangerouslySetInnerHTML 均安全**：
  - `SubgridTool.tsx:750` — 输出 explain 字符串，内部生成，无用户输入注入风险
  - `LightDarkTool.tsx:645` — 输出 explainText，内部生成，配套 `escapeHtml` 函数
  - `MarkdownTool` — 配套 `escapeHtml` + `sanitizeUrl` 协议白名单
  - `SqlTool` 的 `highlightSql` — 对所有用户输入片段完整转义
- **ViewTransitionTool 的 innerHTML**（389、394 行）在 iframe `sandbox="allow-same-origin allow-scripts"` 内执行，且 stateA/stateB 仅来自预设（不可用户编辑），安全
- **iframe 沙箱**：ScopeTool、ScrollTimelineTool 使用 `sandbox="allow-same-origin"`，预览 HTML 可编辑但隔离在 iframe 内
- **EXIF 解析**：ExifTool 使用 `URL.createObjectURL` + `revokeObjectURL` 防内存泄漏，文件大小上限 50MB，exifr 本地解析无网络请求
- **ReDoS 防护**：RegexTool 已在前一日修复中补齐 effectiveText 截断（提交 bd21e2d）；jsonPath/jsonSchema 限制了正则与字符串长度上限
- **客户端/服务端边界**：100 个 .astro 页面 `client:load` 指令使用一致，SSR 阶段无浏览器 API 误调用
- **SEO 完备**：BaseLayout 动态生成 title/description/canonical/og/twitter，blog 文章页有 BlogPosting JSON-LD 结构化数据，sitemap 与 robots.txt 配置正确
