# Bug 检查与修复报告 - auto-website

## 检查时间
2026-07-25 00:50

## 检查范围

### 自动化检查
- `npm run check`（astro check）：基线通过 — 0 errors / 0 warnings / 4 hints（hints 均为历史已知项：`scripts/seo-audit.mjs` 未使用的 import、`src/utils/clipboard.ts` 中 `document.execCommand` 已废弃提示，非本次变更引入）。
- `npm run build`：基线通过 — 981 page(s) built in 32.47s，`[build] Complete!`。
- 修复后 `npm run check`：通过 — 0 errors / 0 warnings / 4 hints（与基线一致）。
- 修复后 `npm run build`：通过 — 984 page(s) built in 23.07s（页面数较基线 +3，源于 HEAD~1 提交新增的博客 tag 页面，与本次修复无关）。

### 最近一次提交变更分析（git diff HEAD~1）
HEAD~1 提交 `bb019b9 feat: 新增数值的内存表示协同博客`，变更内容：
- 新增 `memory/20260724/topics.md`、`src/content/blog/number-memory-representation-guide.md`
- 5 个工具页（`csv-markdown.astro`、`hex.astro`、`ieee754.astro`、`number-base.astro`、`text-wrap.astro`）各 +3 行，仅在「相关博客」`<ul>` 中追加一个 `<li>` 链接
- 审查结论：纯内容追加，链接指向的博客文件均存在，无逻辑改动、无类型问题、无客户端/服务端边界问题、无 SEO 缺失。

### 代码审查范围
- 加密 / 解密 / 验签 utils：`src/utils/aes.ts`、`src/utils/jwe.ts`、`src/utils/jwtSign.ts`、`src/utils/jwtVerify.ts` — 实现完善，含 PBKDF2 迭代上限 DoS 防护、alg 白名单防 `alg=none` 攻击、RSA1_5 拦截、Concat KDF 完整实现。未发现逻辑错误。
- 全局 `eval` / `new Function` / `dangerouslySetInnerHTML` / `innerHTML` 扫描：21 处命中，逐一审查后确认 `MarkdownTool`（自实现 escapeHtml + sanitizeUrl）、`SqlTool`（注释明确已 escapeHtml）等均安全；`IfFunctionTool` 发现两处真实 XSS 漏洞（见下表）。
- 布局 `src/layouts/BaseLayout.astro`：SEO 元数据完整（canonical / OG / Twitter Card / JSON-LD WebSite + WebApplication / RSS / robots），分页 prev/next 信号规范化，占位域名兜底覆盖，未发现问题。
- 配置 `astro.config.mjs`：`rehypeLazyImages` 幂等递归补齐 `loading="lazy"` 与 `decoding="async"`，`output: 'static'` + `compressHTML` + `inlineStylesheets: 'auto'`，未发现问题。

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|
| 1 | P0 严重 | `src/components/IfFunctionTool.tsx`（第 495 行） | `explainText` 通过 `dangerouslySetInnerHTML` 渲染，其中 `prop` 变量取自用户输入的「目标 CSS 属性」`<input>` 框（`config.property`，组件第 565 行），未经 `escapeHtml` 直接拼入 `<code>${prop}</code>`。攻击者可在属性输入框填入 `<img src=x onerror=alert(1)>` 之类的 payload，注入到原理说明面板并执行。 | 复用文件内已有的 `escapeHtml` 函数（第 346 行）包裹 `prop`，仅改 1 行，新增 1 行说明注释。 | `bc1a33f` |
| 2 | P0 严重 | `src/components/IfFunctionTool.tsx`（第 504 行） | 同上 `explainText` 中 `hitPrediction.reason` 未 escape。`reason` 由 `predictHitBranch` 生成（第 389 行），其中拼接了用户输入的 `b.expression` 解析出的 `key` 与 `expectedVal`（如 `自定义属性 ${key} = "${expectedVal}" 命中 style() 条件`），可被注入恶意 HTML。 | 复用 `escapeHtml` 包裹 `hitPrediction.reason`，与同行已有的 `escapeHtml(hitPrediction.branch.value || 'initial')` 保持一致风格，仅改 1 行，新增 1 行说明注释。 | `bc1a33f` |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| — | — | — | — | 本次检查未发现需要评估的 P0/P1 问题。 |

## 记录的轻微问题（P2，未修复）

- `scripts/seo-audit.mjs` 第 3-4 行：`existsSync`、`normalize`、`sep` 三个 import 未使用（ts(6133)）。属历史已知 hints，非本次变更引入，且为独立脚本不影响运行，按约束 P2 不修复。
- `src/utils/clipboard.ts` 第 39 行：`document.execCommand('copy')` 已废弃（ts(6387)）。已有 `clipboard.ts` 中通过 `navigator.clipboard.writeText` 优先路径并回退到 `execCommand`，废弃提示无法消除，属历史已知 hints，P2 不修复。
- `src/components/LightDarkTool.tsx` 第 466 行：`<code>${config.colorScheme}</code>` 中 `config.colorScheme` 来自 `<select>` 下拉框（受限值，无法直接输入任意字符），理论 XSS 风险极低。同文件未引入 `escapeHtml`，为保持最小修改未处理。建议后续统一引入转义工具。
- `src/components/SubgridTool.tsx` `buildExplain`（第 386/390 行）：`tracksToCss(parent.columns)` 输出由 `<select>`（fr/px/%/auto）与 `<input type="number">` 组成，受限输入无法构造恶意 HTML。同文件未引入 `escapeHtml`，按最小修改原则未处理。
- `src/components/PositionAreaTool.tsx` `buildExplain`：插入变量均来自预设常量或枚举映射（`KEYWORD_MAP`、`SYSTEM_LABELS`），非用户自由输入，无 XSS 风险。

## 验证结果
- 修复后 `npm run check`：通过（0 errors / 0 warnings / 4 hints，与基线一致）
- 修复后 `npm run build`：通过（984 page(s) built，`[build] Complete!`）
- 提交：`bc1a33f` 已推送至 `origin/main`（`cfc4518..bc1a33f HEAD -> main`）
