# Bug 检查与修复报告 - auto-website

## 检查时间
2026-07-17 00:25

## 检查范围

### 自动化检查
- `npm run check`（astro check）：**通过** — 0 errors / 0 warnings / 1 hint（`clipboard.ts:39` `document.execCommand` 已废弃提示，属浏览器 API 历史遗留，不影响功能）
- `npm run build`（astro build）：**通过** — 681 page(s) built in 51.62s，`[build] Complete!`，sitemap 正常生成
- 项目无 lint / test 脚本（Astro 项目）

### 代码审查范围
- 最近一次提交 `14f62d9`（docs: 沉淀第 60 轮 SEO 质量优化进度记录）为纯文档变更（`memory/20260716/topics.md`），无逻辑代码
- `git diff HEAD~1` 涵盖的代码变更：
  - 工作区未提交变更（来自并行样式优化流程）：`about.astro`（page-hero 结构包装）、`blog/[...slug].astro`（h2/h3/h4/img/hr 装饰样式）、`csv-markdown.astro` 与 `exif.astro`（meta description 精简）、`global.css`（prose 排版与 page-hero 大屏响应式），均为纯 CSS/HTML 结构调整或 SEO 文案精简，无 P0/P1 问题，未纳入修复范围
- 新增组件重点审查（2026-07-16 新增、前一轮 07-16 报告 00:40 时点未覆盖的两个工具）：
  - `AnchorPositionTool.tsx`（648 行）— CSS 锚点定位生成器，anchor()/anchor-size()/position-try-fallbacks，8 组预设，iframe srcdoc 沙箱预览
  - `PositionAreaTool.tsx`（610 行）— CSS position-area 3x3 网格定位生成器，物理/逻辑/坐标三套关键字体系，span 跨格，iframe srcdoc 沙箱预览
- 对应页面审查：`anchor-positioning.astro`、`position-area.astro`（client:load 指令、meta 标签、jsonLd、FAQ、相关工具链接均正确）
- 安全维度：eval / new Function / dangerouslySetInnerHTML / innerHTML / document.write / CSP
- 客户端/服务端边界：iframe `sandbox="allow-same-origin"` 隔离，无 `allow-scripts`，定位为纯 CSS 渲染
- 配置：astro.config.mjs（site='https://website.niuzi.asia'，output='static'，compressHTML=true，inlineStylesheets='auto'）

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|
| 1 | P1 | src/components/AnchorPositionTool.tsx | `buildPreviewHtml` 中预览 iframe 的 demo 元素（class `anchor-demo`/`target-demo`）未挂载用户选择器对应的类名/id，导致 `buildCss` 生成的 CSS（`.anchor { anchor-name }`、`.tooltip { position-anchor; anchor() }`）不作用于任何预览节点，预览始终为 flex 居中的静态按钮+盒子，无法体现锚点定位效果。同时样式规则 `${selector.replace(/^([.#]?-?[\w-]+)/, '.$1')...}` 对 `.anchor` 类选择器会生成 `..anchor` 无效选择器（dead CSS，靠 `.anchor-demo` 兜底） | 从用户选择器提取首个类名（`.foo`→`foo`）或 id（`#bar`→`bar`）附加到 demo 元素的 class/id 属性，使生成的 CSS 真实作用于预览节点；样式规则改为直接的 `.anchor-demo`/`.target-demo`，移除会生成无效选择器的正则 | e649639 |
| 2 | P1 | src/components/PositionAreaTool.tsx | 同序号 1，`buildPreviewHtml` 的 demo 元素未挂载用户选择器类名，导致 `buildCss` 生成的 `anchor-name`/`position-area` 等 CSS 不作用于预览节点，3x3 网格下方的"锚点+定位元素"演示区始终为静态布局，无法体现 position-area 定位效果 | 同序号 1 方案，从选择器提取类名/id 附加到 demo 元素 | e649639 |
| 3 | P1 | src/components/PositionAreaTool.tsx | `buildCss` 中 `insetOffset` 与 `resetMargin` 同时启用时输出顺序为 `inset: ${offset}px;` → `margin: 0;` → `inset: auto;`，后者覆盖前者，导致用户配置的 `insetOffset` 偏移被静默丢弃。预设"Popover 弹层"（`resetMargin: true, insetOffset: 8`）默认即触发此 bug | 改为 `else if` 分支：仅当 `insetOffset === 0` 且 `resetMargin` 时才写 `inset: auto`；`margin: 0` 独立判断。修复后 insetOffset=8+resetMargin 输出 `inset: 8px; margin: 0;`（偏移保留、margin 重置） | e649639 |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| 1 | P1→P2 | src/layouts/BaseLayout.astro | 缺少 CSP（Content-Security-Policy）meta 标签，站点无第二道 XSS 防线 | 添加 CSP 可能影响 Astro 岛屿水合（内联脚本）与 React 内联样式，需 `'unsafe-inline'` 才不破坏功能；且 meta 标签不支持 `frame-ancestors` 等指令。属于"缺失安全加固"而非"活跃安全漏洞"（已审查所有 dangerouslySetInnerHTML 均做了转义或为内部生成数据），需评估上线后用 Report-Only 模式观察再切换强制模式（沿用 2026-07-16 评估结论） |
| 2 | P2 | src/components/PositionAreaTool.tsx | "Popover 弹层"预设的 `targetSelector: '[popover]'` 为属性选择器，本次预览修复的正则（`^\.([\w-]+)/^#([\w-]+`）不匹配属性选择器，该预设下 demo 元素仍无法挂载对应选择器，预览不体现定位（其余 7 个类选择器预设均已修复） | `[popover]` 元素默认 `display:none` 需 `showPopover()` API 才显示，iframe 沙箱 `sandbox="allow-same-origin"` 不含 `allow-scripts` 无法调用；强行添加 `popover` 属性会改变 demo 元素行为。属性选择器预览属边界场景，生成的 CSS 代码本身正确，需评估是否为属性选择器单独设计 demo 节点 |

## 记录的轻微问题（P2/P3，未修复）

- **[P2]** `src/components/AnchorPositionTool.tsx` / `PositionAreaTool.tsx` — `handleCopy` 中 `setTimeout(() => setCopied(false), 1500)` 卸载后仍 setState（沿用既有反模式，功能正常，需统一 hook 治理）
- **[P3]** `src/components/PositionAreaTool.tsx:585` — `dangerouslySetInnerHTML` 渲染 `buildExplain` 输出，内容全部来自内部固定映射表（KEYWORD_MAP/SYSTEM_LABELS 等），无用户输入注入风险，安全
- **[P3]** `src/components/AnchorPositionTool.tsx` — `explains` 使用 `{tip}` 文本节点渲染（非 dangerouslySetInnerHTML），自动转义，安全
- **[P3]** `src/components/PositionAreaTool.tsx` — `applyPreset` 直接 `setConfig({ ...preset.config })` 未深拷贝 decls（AnchorPositionTool 重新生成了 id），当前 PositionAreaTool 无 decls 列表字段，无引用共享风险
- **[P3]** `src/utils/clipboard.ts:39` — `document.execCommand('copy')` 已废弃（check hint），有 `navigator.clipboard.writeText` 兜底，功能正常

## 验证结果
- 修复后 `npm run check`：**通过**（0 errors / 0 warnings / 1 hint，`clipboard.ts:39` `document.execCommand` 已废弃提示）
- 修复后 `npm run build`：**通过**（681 page(s) built in 22.59s，`[build] Complete!`，sitemap 正常生成）
- 修复已提交并推送：`e649639` → `origin/main`（`7ee6924..e649639`）

## 安全审查结论

项目整体安全基线良好（沿用 2026-07-16 结论，本轮新增两个组件未引入新风险）：
- **无 eval / new Function / setTimeout(stringArg) / setInterval(stringArg)** 调用
- **无 document.write / insertAdjacentHTML** 调用
- **PositionAreaTool 的 dangerouslySetInnerHTML**：`buildExplain` 输出全部来自内部固定映射表（KEYWORD_MAP/SYSTEM_LABELS/AxisSpec 描述映射），config 字段为 TS 枚举类型无法注入用户输入，安全
- **AnchorPositionTool 的 explains**：使用 `{tip}` 文本节点渲染，自动转义，安全
- **iframe 沙箱**：两个工具均使用 `sandbox="allow-same-origin"`（不含 `allow-scripts`），预览 HTML 隔离在 iframe 内，定位为纯 CSS 渲染（anchor-positioning/position-area 无需 JS）
- **客户端/服务端边界**：两个工具页面 `client:load` 指令使用正确，SSR 阶段无浏览器 API 误调用
- **SEO 完备**：两个新工具页 meta title/description/jsonLd 齐全，FAQ 与相关工具链接正确，sitemap 已收录
