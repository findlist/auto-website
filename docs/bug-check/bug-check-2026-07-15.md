# Bug 检查与修复报告 - auto-website

## 检查时间
2026-07-15 00:30

## 检查范围

### 自动化检查
- `npm run check`（astro check）：**通过** — 0 errors / 0 warnings / 1 hint（`clipboard.ts:39` `document.execCommand` 已废弃提示，属浏览器 API 历史遗留，不影响功能）
- `npm run build`（astro build）：**通过** — 552 page(s) built in 61.25s，`sitemap-index.xml` 正常生成
- 项目无 lint / test 脚本（Astro 项目）

### 代码审查范围
- 最近一次提交变更：`git diff HEAD~1`（仅 `src/styles/global.css` 阴影分层与按钮交互微调 + `memory/20260714/topics.md`，无逻辑问题）
- 最近 8 次提交涉及的新增/修改文件：LayerTool、NestingTool、AnimationTool、ContainerTool、WritingModeTool、ScrollSnapTool、jwtVerify 等
- 安全维度：eval / new Function / dangerouslySetInnerHTML / innerHTML / document.write / CSP / robots.txt / RSS / sitemap / og+twitter meta / canonical
- 解析类工具 ReDoS 与原型链污染：jwtSign / jwtVerify / jsonPath / jsonSchema / sql / aes / jwe
- 工具组件逻辑：16 个核心组件（加密、解析、正则、SQL、JWT、CSS 生成器等）
- 客户端/服务端边界：`client:` 指令使用、SSR 阶段浏览器 API 调用
- SEO：BaseLayout meta 标签、blog/[...slug].astro 结构化数据、首页 description

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|
| 1 | P1 | src/components/RegexTool.tsx:230-237 | `runMatch` / `runReplace` 传入完整 `text` 而非截断后的 `effectiveText`，导致文件头注释声称的"输入长度上限 10000 字符（防止 ReDoS）"未真正生效。用户粘贴超长文本（>10000）配合病态正则（如 `(a+)+$`）可冻结浏览器主线程。`effectiveText` 已在下方定义但仅用于高亮预览，匹配/替换仍用完整 `text`。 | 将 `overLimit` / `effectiveText` 计算前移到 `matchResult` useMemo 之前，`runMatch` 与 `runReplace` 统一改用 `effectiveText`，useMemo 依赖同步更新。行为与已存在的"已超上限截断"提示一致。 | bd21e2d |
| 2 | P1 | src/components/AesTool.tsx:91-123 | `handleEncrypt` / `handleDecrypt` 缺少 try/finally 包裹。虽然 `utils/aes.ts` 内部已用 try/catch 捕获并返回错误对象，但若未来代码变更或浏览器 API（crypto.subtle）在非安全上下文抛出未预期异常，`setLoading(false)` 不会执行，按钮将永久卡在"处理中..."禁用态，属"异常处理缺失"。 | 用 `try { ... } finally { setLoading(false); }` 包裹两个 handler 主体，保证 loading 状态在任何情况下都被复位。未新增 catch（保留异常向上传播给 React 错误边界），仅确保状态一致性。 | bd21e2d |

### 修复验证
- 修复后 `npm run check`：**通过**（0 errors / 0 warnings / 1 hint）
- 修复后 `npm run build`：**通过**（564 page(s) built in 20.16s）
- 修复被并行自动开发流程拾取并提交为 `bd21e2d`，已推送至 `origin/main`

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| 1 | P1→P2 | src/layouts/BaseLayout.astro:72-114 | 缺少 CSP（Content-Security-Policy）meta 标签，站点无第二道 XSS 防线 | 添加 CSP 可能影响 Astro 岛屿水合（内联脚本）与 React 内联样式，需 `'unsafe-inline'` 才不破坏功能；且 meta 标签不支持 `frame-ancestors` 等指令。属于"缺失安全加固"而非"活跃安全漏洞"（已审查所有 dangerouslySetInnerHTML 均做了转义），需评估上线后用 Report-Only 模式观察再切换强制模式 |
| 2 | P2 | src/components/ContainerTool.tsx:629 | 维度 ButtonGroup 的 `disabled={... ? false : false}` 为死代码三元，恒为 false。原意是 inline-size 容器禁用 height 维度，但 `disabled` 作用于整个 ButtonGroup，若改为 true 会锁死用户无法切回 width | 正确修复需 ButtonGroup 组件支持单选项禁用（per-option disabled），属于组件重构，超出"最小必要代码修复"范围 |
| 3 | P2 | src/components/ContainerTool.tsx:297-306 | `matchedQueries` 仅按 `previewWidth` 判断命中，未处理 `dimension === 'height'` 的查询，height 维度查询的预览命中判定与生成的 CSS 语义不一致 | 预览限制，生成的 CSS 本身正确；修复需引入预览高度变量与双向拖拽，改动较大 |
| 4 | P2 | src/components/ContainerTool.tsx:378-395 | `addQuery` / `removeQuery` 在 `setQueries` updater 内调用 `setSelectedId`，React 18 严格模式下 updater 会执行两次（反模式） | 功能正常（setSelectedId 幂等），仅是反模式；修复需拆分 updater 或用 useEffect 同步，改动周边代码 |
| 5 | P2 | src/components/AnimationTool.tsx:225-227 | useEffect 中 `setPreviewKey((k) => k + 1)` 触发额外渲染，每次改 keyframe/config 会渲染两次 | 性能反模式，功能正确；修复需将 previewKey 改为派生值或在用户回调中直接设置，属行为重构 |
| 6 | P2 | src/components/RegexTool.tsx:195-206 | `runReplace` 替换字符串处理顺序为 `$<name>` → `$1` → `$$`，若命名组值包含 `$1` 字符串会被二次替换 | 边界场景，需重构为单次扫描 token 解析；当前顺序对常见用例无影响 |
| 7 | P2 | src/components/JwtSignTool.tsx:187-216 | `handleGenRsaKey` / `handleGenEcKey` / `handleSign` 异步操作无 AbortController 或 mountedRef 保护，组件卸载后 setState 可能作用于已卸载组件 | React 18 不再 warn 且不影响功能；修复需引入 ref 标志，属防御性增强 |
| 8 | P2 | src/components/RegexBenchmarkTool.tsx:371-388 | `setTimeout` 未在卸载时 clearTimeout，连续点击会触发多次 setTimeout 导致结果错乱 | 防御性清理，功能基本正常；修复需 useRef 保存 timer id + cleanup |
| 9 | P2 | src/pages/index.astro:11 | 首页 meta description 约 1000+ 字符，列举 60+ 工具名，远超 Google 推荐的 160 字符，存在关键词堆砌风险 | SEO 优化非 bug；精简需重新撰写文案，属内容调整 |

## 记录的轻微问题（P2/P3，未修复）

- **[P3]** `src/components/LayerTool.tsx:484` — `moveLayer` 内 `const target = idx + dir` 与外部 state `target`（级联分析选择器）同名遮蔽，当前作用域不冲突但易致维护误解
- **[P3]** `src/components/ScrollSnapTool.tsx:311` — `display: isBoth ? 'flex' : 'flex'` 三元两侧相同，死代码；若 `both` 应为 `grid` 则是 bug，需确认意图
- **[P3]** `src/components/AnimationTool.tsx:266` — `updateKeyframe` 的 `field: keyof KeyframeState` 包含 `'id'`，误传会覆盖 string 类型 id 为 number；建议改为 `Exclude<keyof KeyframeState, 'id'>`
- **[P3]** `src/components/SqlTool.tsx:176` — `Number(e.target.value) as 2 | 4` 不安全类型断言，当前 select 只有 2/4 选项，运行时安全
- **[P3]** `src/components/JsonSchemaTool.tsx:208` — 空格键未 `preventDefault`，按空格触发错误路径点击时会滚动页面
- **[P3]** `src/components/HashTool.tsx:163-167` — `useMemo` 中 `new TextEncoder()` 在 SSR 阶段执行，Node 18+ 全局可用但旧版本有风险
- **[P3]** `src/components/JwtTool.tsx:139,145` / `JwtVerifyTool.tsx:66` — `atob` 在 SSR 中依赖 Node 18+ 全局，当前初始 input 为空提前返回不触发，但未来载入示例 token 可能崩溃
- **[P3]** 多个组件 — `setTimeout(() => setCopied(false), 1500)` 散布在几乎所有工具组件中，卸载后仍 setState；建议统一通过 `useFlashState` 自定义 hook 管理

## 验证结果
- 修复后 `npm run check`：**通过**（0 errors / 0 warnings / 1 hint）
- 修复后 `npm run build`：**通过**（564 page(s) built in 20.16s，sitemap 正常生成）
- 修复提交 `bd21e2d` 已推送至 `origin/main`

## 安全审查结论

项目整体安全基线良好：
- **无 eval / new Function / setTimeout(stringArg) / setInterval(stringArg)** 调用（仅 blog 文档中作为说明文字）
- **无 document.write / insertAdjacentHTML** 调用
- **2 处 dangerouslySetInnerHTML 均安全**：MarkdownTool 配套 `escapeHtml` + `sanitizeUrl` 协议白名单；SqlTool 的 `highlightSql` 对所有用户输入片段完整转义
- **解析类工具无原型链污染**：jsonPath 用 `hasOwnProperty` 校验、jsonSchema 用 `Object.keys` 不枚举原型链、jwtSign/jwtVerify 基于 Web Crypto API
- **ReDoS 防护**：jsonPath/jsonSchema 限制了正则与字符串长度上限；RegexBenchmarkTool 有完善的超时与静态分析；RegexTool 已在本轮修复中补齐 effectiveText 截断
- **SEO 完备**：BaseLayout 动态生成 title/description/canonical/og/twitter，blog 文章页有 BlogPosting JSON-LD 结构化数据，sitemap 与 robots.txt 配置正确
