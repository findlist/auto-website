# Bug 检查与修复报告 - auto-website

## 检查时间
2026-07-12 00:24

## 检查范围

### 基线检查
- `npm run check`（astro check）：通过（0 errors, 0 warnings, 1 hint）
  - hint 为 `clipboard.ts` 中 `document.execCommand('copy')` 已废弃提示（兼容性回退，非 bug）
- `npm run build`：通过（exit 0，14.23s 构建完成，全部页面与静态资源生成）
  - 注意：构建末尾出现非致命的 `ENOENT: dist\favicon.svg` 告警（Astro 在 Windows 上 `removeEmptyDirs` 的竞态，文件实际存在于 dist 中，exit code 为 0，非代码 bug）

### 代码审查范围
- **src/components/**：48 个工具组件，重点审查最近添加的 FindReplaceTool、TruncateTool、SlugTool、ReverseTool、SortTool、RandomPickerTool，以及 setInterval/setTimeout 清理逻辑（CronTool、TimestampTool）
- **src/pages/**：博客分页页 `[...page].astro`（最近新增）、博客详情页 `[...slug].astro`、RSS、sitemap 输出
- **src/utils/**：jsonPath.ts（验证上一轮 P1 修复）、site.ts、tags.ts
- **src/layouts/BaseLayout.astro**：分页 SEO 信号（prevUrl/nextUrl）、OG 图 meta 标签
- **src/styles/global.css**：CSS 变量定义完整性
- **astro.config.mjs / content.config.ts / public/robots.txt**：配置与 SEO 资源
- **git diff HEAD~1 / HEAD~2**：最近变更（博客分页功能 commit 837c85c）
- **git diff（未提交变更）**：auto-dev 正在进行的 CSS 变量重构与新增 MorseTool/TextSimilarityTool

### 安全检查
- `eval / new Function`：未使用（仅博客文章中作为反面教材提及）
- `innerHTML / dangerouslySetInnerHTML`：2 处（MarkdownTool、SqlTool），均已做 XSS 防护（上一轮已审查）
- `set:html`：1 处（BaseLayout JSON-LD），使用 JSON.stringify 输出，安全
- CSP 违规：无内联事件处理器，无外部脚本注入

### 上一轮 P1 问题复验
- **jsonPath.ts 过滤表达式根路径引用**（上一轮标记"需评估"）：已由 auto-dev 修复。`rootData` 参数已正确传递整个调用链（evaluate → evaluateSegment → evaluateFilter → evaluateFilterNode → evaluateCompare → evaluateOperand），`$` 根路径引用现在能正确求值。
- **og-image SVG 格式不兼容社交平台**（上一轮标记"需评估"）：已由 auto-dev 修复。`public/og-image.png` 为真实 PNG 图片（1200x630, 8-bit colormap），BaseLayout 已补充 `og:image:width/height/type` meta 标签。

## 已修复的问题

无。本轮检查未发现需要修复的 P0/P1 问题。

上一轮报告中的两个 P1 问题（jsonPath 根路径、og-image SVG）均已被 auto-dev 在后续开发中修复，本轮复验确认修复正确。

## 未修复的问题（需评估）

无。上一轮标记"需评估"的两个 P1 问题均已解决，本轮未发现新的需要评估的问题。

## 记录的轻微问题（P2，未修复）

- **clipboard.ts:39** `document.execCommand('copy')` 已废弃（check hint），已有 navigator.clipboard 回退逻辑，兼容性处理得当
- **构建末尾 ENOENT 告警**：`npm run build` 在 Windows 上构建完成后，Astro 内部 `removeEmptyDirs` 函数尝试 stat `dist/favicon.svg` 时触发 ENOENT。文件实际存在，exit code 为 0，是 Astro 在 Windows 平台的已知竞态告警，不影响构建产物
- **auto-dev WIP 未提交变更**：检测到 auto-dev 正在并行进行 CSS 变量重构（将 json.astro/jwt.astro/blog 页面中的硬编码 rgba 值迁移至 CSS 变量）和新增 MorseTool/TextSimilarityTool 组件。所有新引用的 CSS 变量（`--color-primary-border`、`--color-accent-soft`、`--color-success-border`、`--color-success-ring`）均已在 global.css 中定义（含暗色模式覆盖），WIP 代码正确，未干预
- **首页 /json/ 带尾斜杠**而其余工具链接不带，一致性小问题不影响功能（上一轮已记录）
- **tsconfig.json** `exclude: ["dist"]` 覆盖了 strict 默认的 node_modules 排除（TypeScript 隐式排除仍生效，上一轮已记录）

## 验证结果
- 修复后 check：无需修复，基线 check 已通过（0 errors, 0 warnings, 1 hint）
- 修复后 build：无需修复，基线 build 已通过（exit 0，全部页面生成）
- 无需提交（未发现需要修复的 P0/P1 问题，不创建空提交）
