# Bug 检查与修复报告 - auto-website

## 检查时间
2026-07-13 00:27

## 检查范围

### 基线检查
- `npm run check`（astro check）：通过（0 errors, 0 warnings, 1 hint）
  - hint 为 `clipboard.ts:39` 中 `document.execCommand('copy')` 已废弃提示（兼容性回退，非 bug）
- `npm run build`：通过（exit 0，16.68s 构建完成，408 页面全部生成）

### 代码审查范围
本轮重点审查自上次 bug-check（2026-07-12 00:24）以来新增的 9 个工具组件及其页面、工具函数：

**新增工具组件**（commit 5bc6761 ~ 5f2ea7b）：
- `src/components/Ieee754Tool.tsx`：IEEE 754 浮点数可视化（DataView + BigInt 位级 reinterpret）
- `src/components/HtmlToMarkdownTool.tsx` + `src/utils/htmlToMarkdown.ts`：HTML 转 Markdown（DOMParser 递归渲染）
- `src/components/CsvMarkdownTool.tsx`：CSV 与 Markdown 表格互转（手写状态机解析）
- `src/components/ImageCompressTool.tsx`：图片压缩（Canvas API + ObjectURL）
- `src/components/PasswordHashTool.tsx`：密码哈希（bcryptjs + Web Crypto PBKDF2）
- `src/components/ExifTool.tsx`：EXIF 元数据查看器（exifr）
- `src/components/BoxShadowTool.tsx`：CSS 盒阴影生成器
- `src/components/GradientTool.tsx`：CSS 渐变生成器
- `src/components/TextShadowTool.tsx`：CSS 文字阴影生成器

**对应页面**：ieee754.astro、html-to-markdown.astro、csv-markdown.astro、image-compress.astro、password-hash.astro、exif.astro、box-shadow.astro、gradient.astro、text-shadow.astro

**配置与样式**：确认 global.css 暗色模式实现为 `@media (prefers-color-scheme: dark)`（非 `.dark` 类）

### 安全检查
- `eval / new Function`：未使用
- `innerHTML / dangerouslySetInnerHTML`：本轮新增组件均未使用
- `set:html`：本轮新增页面均未使用
- CSP 违规：无内联事件处理器，无外部脚本注入
- 密码哈希：PBKDF2 验证使用常数时间比较，盐值使用 CSPRNG 生成，安全实现正确

### 客户端/服务端边界
- 所有新增 React 组件均使用 `client:load` 指令，水合时机正确
- HtmlToMarkdownTool 与 CsvMarkdownTool 的实时转换放在 `useEffect` 中，避免 DOMParser 在 SSR 阶段不可用导致的 hydration mismatch，处理正确

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|------|----------|------|----------|----------|----------|
| 1 | P1 | src/components/CsvMarkdownTool.tsx | CSV 转 Markdown 时，若取消勾选"首行为表头"（hasHeader=false），第一行数据被丢弃。原因：`stringifyMdTable` 函数数据行循环使用 `Math.max(dataStart, 1)` 作为起始索引，当 hasHeader=false 时 dataStart=0 被 Math.max 强制为 1，导致索引 0 的数据行被跳过 | 将循环起始索引从 `Math.max(dataStart, 1)` 改为 `dataStart`，确保无表头模式下所有行均作为数据行输出 | 6975f55 |

## 未修复的问题（需评估）

无。本轮未发现需要评估的 P0/P1 问题。

## 记录的轻微问题（P2，未修复）

- **clipboard.ts:39** `document.execCommand('copy')` 已废弃（check hint），已有 `navigator.clipboard` 回退逻辑，兼容性处理得当
- **CsvMarkdownTool.tsx handleSample**（第 433-438 行）：在 `setMode` 的状态更新函数内调用 `setInput`（副作用），是 React 反模式。StrictMode 下 updater 可能调用两次，但因 setInput 幂等且 setMode 返回 prev 不变，实际无功能影响
- **CsvMarkdownTool.tsx isSeparatorCell**（第 312 行）：`cell.includes('-')` 检查与正则 `/^:?-+:?$/` 中的 `-+` 重复，冗余但无害
- **csv-markdown.astro**（第 396-398 行）：`:global(.dark) .csvmd__textarea--output` 规则为死代码。项目暗色模式通过 `@media (prefers-color-scheme: dark)` 实现（见 global.css 第 73 行），不存在 `.dark` 类。该规则设置与基础规则相同的 `var(--color-bg-soft)`，不影响功能
- **ImageCompressTool.tsx 卸载清理**（第 375-382 行）：`useEffect` 使用空依赖数组 `[]`，cleanup 闭包捕获的 `source`/`result` 恒为初始值 null，卸载时不释放当前 ObjectURL。因 Astro 全页面加载会自动回收 URL，实际无内存泄漏，但代码意图与行为不符
- **htmlToMarkdown.ts parsererror 检查**（第 137 行）：`doc.querySelector('parsererror')` 在 `text/html` 模式下永不触发（DOMParser 仅在 XML/SVG 模式产生 parsererror），为死代码。不影响功能，HTML5 容错解析已处理非法标签
- **BoxShadowTool / GradientTool / TextShadowTool 模块级计数器**：`let layerIdSeq = 1` / `let stopIdSeq = 1` 为模块级可变变量，在组件多次挂载时 ID 持续递增。功能正确，仅 ID 数字非连续，无实际影响
- **auto-dev WIP 未提交变更**：检测到 auto-dev 正在并行新增 BorderRadiusTool、TransformTool 及修改 jwt/regex 等页面，所有 WIP 文件未提交，本轮未干预

## 验证结果
- 修复后 check：通过（0 errors, 0 warnings, 1 hint）
- 修复后 build：通过（exit 0，408 页面构建完成）
- 提交：6975f55 已推送至 origin/main
