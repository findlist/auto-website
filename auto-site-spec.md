# auto-website 自主进化 Agent 规范

> 版本：v2.0
> 更新日期：2026-07-28
> 阶段：已上线工具站的数据与搜索信号驱动维护期

## 成功标准

Agent 只处理可验证的用户需求、线上故障、搜索表现、性能、安全或可访问性问题。工具数量、SEO 文案修改次数、拆分模块数量、提交数和迭代轮次不是成功指标。无合格任务时零修改、零提交结束。

禁止反复精简 title/description、拆分模块、补写轮次文档或新增无需求工具来维持自动迭代。

## 有效信号

- 线上错误、功能故障、错误计算结果或用户反馈。
- CI、测试、构建、链接检查和依赖安全扫描失败。
- Search Console 的收录、点击率或排名出现有统计意义的变化。
- 访问统计显示的搜索需求、失败路径或高退出页面。
- Core Web Vitals、Bundle 体积或运行性能退化。
- 明确批准的新工具或内容任务。

没有 Search Console、访问数据或用户反馈支持的 SEO 猜测不得自动实施。

## 选择门槛

候选按 0-5 分评估：用户价值 V、严重度 S、证据 C、紧迫度 U、风险 R、成本 E。

`Score = 3V + 3S + 3C + U - 2R - E`

自动实施必须满足：`Score >= 24`、`C >= 4`、`R <= 2`，并有明确验收标准。单次最多实施最高分的 1 项；无合格项立即停止。

## 权限边界

可自动实施：可复现工具缺陷、错误结果、失败测试或构建、明确性能回归和局部可访问性问题。

必须授权：新增工具、改变计算语义、引入统计或广告、隐私和 Cookie 变化、依赖变化、站点导航重构、大规模 SEO 或内容改写。

默认拒绝：无数据支撑的 SEO 文案轮换、重复结构化数据微调、纯模块拆分、常量抽取和仅用于沉淀轮次的文档提交。

## 运行与验证

1. 默认每天只读评估一次；没有新增部署、告警或指标变化时直接跳过。
2. 最多列出 5 个候选并评分。
3. 无合格任务时不得修改文件或生成日报。
4. 实施前写明用户影响和验收标准。
5. 运行相关测试和构建；计算逻辑、共享组件或构建链变化时扩大验证范围。
6. Cloudflare Pages 随 push 自动部署；只有 `R <= 1` 且 `npm run check`、完整生产构建和相关测试全部通过时才允许自动 push。

## Git 与记忆

- 达到评分门槛、`C >= 4`、`R <= 1`、工作区干净、`npm run check` 与完整生产构建全部通过，且不涉及授权范围时，必须自动按单任务 commit 并执行 `git push origin HEAD`。
- 自动推送前必须再次检查工作区和暂存文件，只能精确 `git add <本任务文件>`，禁止 `git add -A`。
- 单次最多创建并推送 1 个提交；push 失败时保留当前提交并报告，不追加提交或反复重试。
- `R = 2` 的任务不得自动提交或部署。
- 工作区启动时不干净则禁止自动提交和推送；保留已有修改，禁止 force push、`reset --hard`、`clean -f`。
- 长期状态只记录指标基线、授权任务、未解决问题和用户决策；单次更新不超过 40 行。
- 无新增信号不得创建 bug-check、style-optimization 或轮次摘要文件。

## 输出

```markdown
## 自主进化评估
- 新增信号：无 / 简述
- 最高分候选：无 / 任务名（分数）
- 决策：实施 / 提案待授权 / 零修改结束
- 验证：无 / 结果
- 用户决策：无 / 具体事项
```
